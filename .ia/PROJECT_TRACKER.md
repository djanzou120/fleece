# Fleece — Project Tracker (mémoire de l'agent PM)

> **Propriétaire : `fleece-pm`.** Source de vérité du suivi projet : tâches, assignations,
> tests d'acceptance, blocages. Mis à jour à chaque intervention/changement d'état.
> Voir aussi `.ia/MEMORY.md` (décisions techniques) et `.ia/user-story.md` (critères d'acceptance).
>
> Dernière mise à jour : **2026-07-13** (T-016 Done/PASS — routing `highest_delivery` combine le score provider et le score de joignabilité Contact Intelligence via client REST interne (port C2 `ContactScoreClient`, `CombineScore` pur, fallback gracieux si contact-intelligence indisponible). QA PASS 9/9, architecture CONFORME, isolation cross-service vérifiée. **Vague 2 V1 : T-014 ✅, T-016 ✅ — reste T-018 → T-020 (+ T-038 seed Telegram, prérequis de T-015).** — T-014 Done/PASS — adapter provider Telegram `src/provider` (`telegram.go` + registry `telegram-bot`, mapping statuts, coût 0, capabilities Go, token config/env). QA PASS 9/9 (64 tests), architecture CONFORME. Débloque T-015. — T-019 Done/PASS — migration `0017_analytics_kpi.sql` (schéma `analytics` : latence somme, MV `kpi_daily` 4 KPIs produit, index UNIQUE pour REFRESH CONCURRENTLY). QA PASS 8/8. Débloque T-020. **✅ VAGUE 1 V1 COMPLÈTE : T-011/T-012/T-013/T-017/T-019 tous Done/PASS.** — T-017 Done/PASS — migration `0016_campaign_execution.sql` (schéma `campaign` : message/stratégie/estimated_cost/rate_limit, statut+dédup par destinataire, compteurs de run, index scheduler). QA PASS 7/7. Débloque T-018. — T-013 Done/PASS — migration `0015_provider_telegram.sql` (schéma `provider`, seed provider `telegram-bot`, credentials via pattern `provider_credentials` existant, capabilities côté adapter Go). QA PASS 8/8. Débloque T-014. — T-012 Done/PASS — service Contact Intelligence `src/contact-intelligence` livré (Vague 1 V1) : domaine scoring Laplace, use cases Get/Enrich/RecordOutcome, upsert transactionnel, consumer DLR, endpoints GET score/POST outcomes, port 8086. QA PASS 8/8 (29 tests, 0 régression), architecture CONFORME. Débloque T-016. Dette pré-existante relevée : depguard/golangci absent du dépôt. — Remboursement de dette transverse PASS/CONFORME sur branche `chore/tech-debt-paydown` : wallet `ListTransactions`+endpoint, BFF SDL `transactions`/`rotateApiKey`/`createWorkspace`/`secretPreview`, session dashboard (fin de `WORKSPACE_ID_FROM_SESSION`), harmonisation `@anthill`→`@fleece`, CI `rest-api`, Makefile `k8s-configmaps`. Dettes D-E01/02/03/06, T-007.4, T-008.1a/c/f, T-008.4, T-009.2/.4 soldées ; résiduels réseau documentés. Revue backlog V1 : ajout T-038 (seed Telegram, scission T-015) + ordre d'exécution en 5 vagues. — **Affinage backlog V2** : scissions d'atomicité (T-039 schéma SSO, T-040 seed Messenger/RCS, T-041 schémas A/B, T-042 analytics variantes) + track API REST publique **D25** (T-043 impl rest-api, T-044 rate limiting Redis, T-045 DevOps) ; T-026/T-031/T-033/T-035/T-037 ajustés ; ordre d'exécution V2 en 8 vagues (A→H). — T-011 Done/PASS (migration `0014_contact_intel_scoring.sql`). — Backlog V1 🟡 (T-011..T-025, T-038) + V2 🔵 (T-026..T-037, T-039..T-045) ; MVP P0 T-001..T-010 Done/PASS. — T-010 Done/PASS — QA : suite d'acceptance MVP statique/structurelle offline-safe. Sections A-E PASS, 0 bloquant. `go build`/`go vet ./src/...` exit 0 ; `go test ./src/...` **161/161 PASS** (15 packages). SDL GraphQL 5 Query/4 Mutation/7 types OK ; 6/6 routes auth-api OK ; 6/6 use cases (HMAC/backoff/solde≥0/4 stratégies/SHA-256/ApiContext) OK ; frontend 9/9 ops alignées SDL, rawKey 1×, secret masqué. **MVP P0 clôturé** : T-001..T-010 toutes Done/PASS. 7 dettes non bloquantes recensées (déjà tracées T-007/T-008/T-009).

## Légende
- **Statut** : `Backlog` · `Ready` · `In Progress` · `In Review` · `Blocked` · `Done`
- **Acceptance** : `—` (non testé) · `PASS` · `FAIL`
- **Phase** : 🟢 MVP (P0) · 🟡 V1 (P1) · 🔵 V2 (P2)

## Équipe d'agents
| Agent | Domaine |
|-------|---------|
| `fleece-pm` | Orchestration, suivi, acceptance, ce tracker |
| `fleece-go-engineer` | Services Go (cœur métier) |
| `fleece-ts-engineer` | `auth-api` (Identity/Better Auth), `graphql-api` (BFF) |
| `fleece-frontend-engineer` | Dashboard `platform-app` (Next.js) |
| `fleece-db-engineer` | Schéma + migrations Atlas |
| `fleece-devops-engineer` | Makefile/Docker/K8s/CI/infra |
| `fleece-qa-engineer` | Tests & exécution des critères d'acceptance |
| `fleece-architect-reviewer` | Garde-fous Clean Architecture (lecture seule) |

---

## État global (baseline)
Le scaffold de l'architecture est en place et compile (`go build ./src/...` OK ; `make build pkg=messaging` OK).
Les couches Clean Architecture existent pour tous les services Go et TS, mais la plupart des adapters/use cases
sont des squelettes (`doc.go` / `TODO`). Le service **messaging** est rempli comme référence.

---

## Backlog / Tâches

| ID | Tâche | Service / zone | Agent | Phase | Statut | Acceptance | Notes |
|----|-------|----------------|-------|-------|--------|-----------|-------|
| T-001 | Adapters concrets messaging (Postgres, RabbitMQ, HTTP, clients) | `src/messaging` | go-engineer | 🟢 | Done | PASS | Adapters C3 + infra C4 + composition root livrés (stdlib-only, go.mod inchangé). 1 violation de dépendance (adapters→infra via interface Broker) détectée puis corrigée (Broker rapatrié en C3 `adapters/messaging`). TODO(amqp)/driver pq différés. Tests adapters absents (couverture à renforcer plus tard). |
| T-002 | Implémenter Wallet (débit/refund/ledger) | `src/wallet` | go-engineer | 🟢 | Done | PASS | C1 Money/Wallet/Transaction + C2 use cases debit/credit/refund/get_balance + C3 persistence/publisher/http + interface Broker en C3 (`adapters/messaging`) + C4 config/postgres/rabbitmq(NoopBroker)/httpserver + composition root. Stdlib-only, go.mod/go.sum inchangés. 19/19 tests verts (15 domaine + 4 use case debit). Frontières CONFORMES (0 violation C3→C4). Dette : pas de tests use case credit/refund/get_balance ni adapters http/persistence (à renforcer). |
| T-003 | Implémenter Routing (stratégies + fallback) | `src/routing` | go-engineer | 🟢 | Done | PASS | C1 domaine pur (Money, Channel, Strategy, ProviderPricing/Score, RoutingRule, RoutingDecision+ProviderRef, `SelectProvider`) + C2 ports input/output + usecases get_routing_decision/update_provider_score + C3 persistence (schéma `routing`, colonnes exactes 0004)/http (POST /route, POST /scores) + C4 config(port 8083)/postgres/httpserver + composition root. Pas de broker (Routing ne publie pas). Stdlib-only, go.mod inchangé/go.sum absent. 35/35 tests verts (13 domaine + 5 use case + 17 handler ajoutés par QA). Frontières CONFORMES (0 violation C3→C4, isolation schéma). Écarts schéma/spec documentés : devise via config DefaultCurrency (XAF) car absente de provider_pricing ; `Fastest`→fallback score (pas de latence en 0004) ; `Custom`→fallback HighestDelivery (pas de config JSON) ; EstimatedCost unitaire (Messaging multiplie). |
| T-004 | Implémenter Provider + adapters WhatsApp/SMS | `src/provider` | go-engineer | 🟢 | Done | PASS | C1 domaine (OutboundMessage, Money, DeliveryStatus + transitions, Provider, errors) + C2 port `Provider` (Send/EstimateCost/GetDeliveryStatus) + ports ProviderRepository/EventPublisher + usecases Dispatch/HandleDLR + C3 adapters providers (WhatsAppMeta, SMSTwilio stubs net/http TODO(production)) + persistence (schéma `provider`, table provider_messages cols internal_id/provider_id/external_id) + publisher + interface Broker C3 + http handler/dto (POST /dispatch, POST /dlr) + C4 config(8084)/postgres/rabbitmq(NoopBroker)/httpserver + composition root (registry map[string]Provider par providerId). Stdlib-only, go.mod inchangé / go.sum absent. 35/35 tests verts (QA a ajouté handler_test 14, handle_dlr_test 6, sms_twilio_test 8). Frontières CONFORMES (0 violation C3→C4, Broker en C3, isolation schéma `provider`). Écart schéma 0005 (minimal) : Name/Country dans l'entité mais non persistés ; Active↔colonne `enabled` ; pas de colonne status/recipient/content → HandleDLR publiait l'événement sans persister le statut. **Dette DLR status SOLDÉE le 2026-06-19 (D2)** : migration 0012 ajoute `status`/`updated_at` ; `HandleDLR` persiste désormais via `UpdateStatus`. |
| T-005 | Implémenter Webhook (signature HMAC + retries) | `src/webhook` | go-engineer | 🟢 | Done | PASS | C1 domaine pur (WebhookEndpoint+Subscribes, WebhookDelivery, DeliveryStatus Pending/Delivered/Failed/Exhausted, Sign/Verify HMAC-SHA256 stdlib avec hmac.Equal, errors +ErrEmptyPayload) + C2 ports input/output (EndpointRepository/DeliveryRepository/HTTPDispatcher) + usecases deliver_event/retry_delivery (backoff 1m/5m/30m/2h/8h, max 5→Exhausted)/register_endpoint (secret crypto/rand) + C3 persistence (schéma `webhook`, text[] manuel)/dispatcher net/http (X-Fleece-Signature, timeout 10s)/consumer RabbitMQ/interface Broker C3 (`adapters/webhook`)/http (POST/GET/DELETE /endpoints) + C4 config(8085)/postgres/rabbitmq(NoopBroker)/httpserver + composition root. Stdlib-only, go.mod inchangé / go.sum absent. 29/29 tests verts (QA a ajouté register_endpoint_test 7, deliver_event_signature_test 4, array_test 4, handler_test 13). Frontières CONFORMES (0 violation C3→C4, Broker C3 + NoopBroker C4 avec assertion, assertions repos ajoutées). **Dette schéma documentée** : 0006 minimal (pas de active/payload/next_retry_at) → Active=true par défaut à la lecture ; payload/next_retry_at non persistés ; RetryDelivery guard ErrEmptyPayload (piège explicite) + retryUC non câblé à un scheduler. Nécessite migration db-engineer avant scheduler de retry. |
| T-006 | Identity : workspaces, users, API Keys, Better Auth | `src/auth-api` | ts-engineer | 🟢 | Done | PASS | C1 domaine pur (Workspace, ApiKey enrichi keyHash/prefix/permissions/active/lastUsedAt/expiresAt + isExpired/isActive/revoke, User interface, 5 errors WorkspaceNotFound/ApiKeyNotFound/ApiKeyRevoked/InvalidApiKey/Unauthorized), C2 ports (Workspace/ApiKey/User Repository + AuthProvider create/validate/deleteSession types purs) + use cases create-workspace/create-api-key (fk_<prefix>_<random>, SHA-256 node:crypto, ne stocke que le hash, retourne clé claire 1×)/validate-api-key (→ ApiContext @fleece/api-common)/revoke/rotate, C3 adapters/auth Better Auth CONFINÉ (BetterAuthLike stub, TODO offline) + persistence Drizzle stubs (mapping colonnes exactes 0001) + http handler node:http (POST /workspaces, POST/DELETE .../api-keys, POST .../rotate, POST /validate-key, mapping erreurs 400/401/403/404), C4 db/schema.ts (pgSchema identity stub aligné 0001) + better-auth.config.ts stub + composition root index.ts DI manuelle. Offline : stubs typés better-auth/drizzle (aucun import npm actif non installé). tsc --noEmit OK (tsc système). Cycle ts-engineer → architect-reviewer (CONFORME) → qa (PASS structurel 18/18 fichiers, 6/6 fonctionnels, 4/4 garde-fous). **Dette schéma 0001 → db-engineer** : colonnes slug/ownerId/plan, name/role, name/prefix/permissions/last_used_at/expires_at absentes (TODO(schema), valeurs par défaut à la lecture, updateLastUsed no-op). Style imports `.js` (NodeNext). Observations non bloquantes : `paths` tsconfig = béquille offline (risque divergence esbuild/tsc à surveiller) ; package.json nommé `@anthill/auth-api` (à harmoniser `@fleece/`). Build esbuild/`make build pkg=auth-api` non exécuté (offline). |
| T-007 | BFF GraphQL : schéma + résolveurs + clients REST | `src/graphql-api` | ts-engineer | 🟢 | Done | PASS | C2 application (ports/output/clients.ts : IdentityClient/WalletClient/MessagingClient/WebhookClient + 7 DTO + Page<T> de api-common ; use cases get-workspace-overview agrégation Promise.all, list-api-keys, manage-webhook) ; C3 adapters (graphql/schema.graphql MVP complet, context.ts buildContext extrait Bearer/cookie→SessionValidator→ApiContext, 3 résolveurs workspace/wallet/webhook + index buildResolvers, 4 clients REST identity/wallet/messaging/webhook stubs fetch TODO(production) + assertions `_conformance`) ; C4 infrastructure (config.ts port 4000 + URLs services env, session-validator.ts HttpSessionValidator→auth-api /validate-session stub, server.ts stub GraphQLServerLike + câblage schéma/resolvers/context node:http SIGTERM) + graphql.d.ts (declare module "*.graphql") ; composition root index.ts DI manuelle. tsconfig paths @fleece/api-common. **Offline** : Apollo/Yoga/graphql 100% commentés (TODO production), fetch global + node:http seuls actifs. Cycle ts-engineer (tsc --noEmit exit 0 via tsc système 5.9.3) → architect-reviewer (CONFORME 0 violation) + qa (PASS 20/20 fichiers, B/C/D/E PASS, tsc exit 0). Dettes documentées (voir Blocages). |
| T-008 | Dashboard : onboarding, API Keys, Wallet, Webhooks | `src/platform-app` | frontend-engineer | 🟢 | Done | PASS | Stories DASH-01..04. Prérequis devops livré (`mk/react.mk` + `docker/react.dockerfile` + workspace `./src/platform-app`). Scaffold Next.js App Router manuel (offline, 34 fichiers) : config (`next.config.js` `output:'standalone'`, tsconfig `@/*`, package.json `@fleece/platform-app`), couche données `lib/graphql/` (client `fetch`→BFF `NEXT_PUBLIC_GRAPHQL_URL`, queries+types alignés SDL), UI inline shadcn-like (`components/ui/*`, `components/states/*`), layout Sidebar 230px + Header, dark theme tokens DESIGN.md exacts (--accent #27cf7d, --bg #0a0a0a…). DASH-01 signup+stepper 4 étapes (rawKey 1×). DASH-02 liste/create(modal rawKey 1×)/revoke/rotate(=revoke+create). DASH-03 solde walletBalance(centimes/100+currency)/recharger placeholder/historique empty-state. DASH-04 endpoints/add(url+events)/delete/secret masqué/livraisons empty-state. QA PASS (A 34 fichiers, B frontières+types SDL OK aucun appel GraphQL fantôme, C DASH-01..04 OK, D tsc SKIP offline). architect-reviewer CONFORME (0 import backend, données 100% via BFF, type=react intact, appels `/api/auth/*` same-origin Better Auth non bloquants). Dettes : voir Blocages T-008. |
| T-009 | Job d'init K8s `atlas migrate apply` + manifests services + CI | `deploy/k8s/`, `.github/` | devops-engineer | 🟢 | Done | PASS | 16 fichiers. `deploy/k8s/` : namespace `fleece`, config (ConfigMap `fleece-config` + `secret.example.yaml` template), infra (postgres:16 + rabbitmq:3-management, emptyDir dev), jobs/atlas-migrate (image `fleece/bastion:latest`, `atlas migrate apply --dir file:///migrations --url $(DATABASE_URL)`, ConfigMap `atlas-migrations` monté /migrations, initContainer wait-for-postgres, restartPolicy Never), 8 services (Deployment+Service+HPA). Noms Service K8s = DNS exact (messaging/wallet/routing/provider/webhook/auth-api/graphql-api/platform-app), ports 8081-8085/3001/4000/3000, triple cohérence containerPort↔PORT↔Service vérifiée. Env RÉELLES : Go=`POSTGRES_DSN`(libpq key=value via secretKeyRef)+`POSTGRES_SEARCH_PATH`(schéma par service)+`POSTGRES_DRIVER`+`RABBITMQ_URL`(routing exclu) ; routing+`DEFAULT_CURRENCY=XAF` ; auth-api=`DATABASE_URL`(URL) ; graphql-api=AUTH/WALLET/MESSAGING/WEBHOOK_API_URL ; platform-app=`NEXT_PUBLIC_GRAPHQL_URL`. Secret fournit POSTGRES_DSN (Go) ET DATABASE_URL (TS+Atlas) + Twilio/WhatsApp. auth-api/graphql-api ont `command:["node","/usr/local/bin/<pkg>"]` (node.dockerfile dist sans ENTRYPOINT). graphql-api+platform-app=LoadBalancer (NodePort en alt commenté), reste ClusterIP. `.github/workflows/ci.yml` : jobs go (build/vet/test, Go 1.26.4 aligné go.mod), ts (`make build pkg=auth-api|graphql-api`), migrations (`atlas migrate hash`+drift+validate), frontend (`make build pkg=platform-app`). QA PASS A-G (16/16 fichiers, 15/15 YAML parsés pyyaml 0 tabulation, ports/DNS OK, env OK, job Atlas OK, images OK, CI OK). PM a corrigé un commentaire trompeur routing.yaml. **Offline** : kubectl/docker/atlas non exécutés (cluster/réseau absents). Dette : `make build pkg=rest-api` absent du CI (rest-api scaffolé mais hors-scope T-009). |
| T-010 | Suite de tests d'acceptance MVP | transverse | qa-engineer | 🟢 | Done | PASS | Suite d'acceptance statique/structurelle offline-safe (API-01..04, DASH-01..04). Séquence : qa → PM (verdict). **A** SDL GraphQL `schema.graphql` : 5/5 Query (workspace, apiKeys, walletBalance, webhookEndpoints, messages) + 4/4 Mutation (createApiKey, revokeApiKey, createWebhookEndpoint, deleteWebhookEndpoint) + 7/7 types (Workspace, ApiKey, CreateApiKeyResult.rawKey, WalletBalance, WebhookEndpoint, Transaction, Message). **B** routes `auth-api/adapters/http/auth.handler.ts` 6/6 (POST /workspaces, POST/DELETE /workspaces/:id/api-keys/:keyId, POST .../rotate, POST /validate-key, POST /validate-session). **C** 6/6 use cases : create-api-key (rawKey 1×, SHA-256, ne persiste que le hash), validate-api-key (→ ApiContext api_key), webhook deliver_event (HMAC-SHA256 + hmac.Equal), retry_delivery (backoff 1m/5m/30m/2h/8h, max 5→Exhausted), wallet debit (ErrInsufficientFunds, solde≥0), routing selector (lowest_cost/highest_delivery/fastest/custom + ErrInvalidStrategy). **D** `go build`/`go vet ./src/...` exit 0 ; `go test ./src/... -count=1` **161/161 PASS, 0 FAIL** (15 packages : messaging 10, provider 44, routing 54, wallet 19, webhook 48 ; analytics/campaign/contact-intelligence sans tests = scaffold V1/V2) ; go.mod inchangé. **E** frontend : `lib/graphql/queries.ts` 9/9 ops alignées SDL, 0 appel fantôme (rotate=revoke+create UI, transactions/secret en empty-state/placeholder) ; api-keys rawKey affiché 1× (modal détruit à la fermeture) ; wallet solde via `walletBalance` (centimes/100) ; webhooks secret `••••` masqué. Tests jest TS SKIP documenté (offline, node_modules absent → revue statique B/C). 7 dettes non bloquantes (voir Blocages T-010), toutes déjà tracées en T-007/T-008/T-009. **MVP P0 clôturé.** |

### Backlog V1 (🟡 P1) — Contact Intelligence, Telegram, Campagnes, Analytics avancées (~3 mois)

| ID | Tâche | Service / zone | Agent | Phase | Statut | Acceptance | Notes |
|----|-------|----------------|-------|-------|--------|-----------|-------|
| T-011 | Schéma Contact Intelligence — migration additive (score, channels, historique, index) | `migrations/` (schéma `contact_intel`) | db-engineer | 🟡 | Done | PASS | `migrations/0014_contact_intel_scoring.sql` (0013=dernier). STRICTEMENT additive (14× `IF NOT EXISTS`, 0 destructif, 0008 intact via git diff, schéma `contact_intel` seul, 0 FK cross-schéma). Enrichit `contacts` : country VARCHAR(10)/last_channel text/last_success_at TIMESTAMPTZ + compteurs agrégés success_count/failure_count. **Choix : table dédiée `contact_channel_scores`** (phone,channel PK, score 0-100, sample_size, last_success_at, updated_at) plutôt que JSONB — indexabilité `(channel,score DESC)`, lock ciblé sur upsert, lisibilité (D24). 5 index scoring/lecture (country, (country,delivery_score DESC), phone, (channel,score DESC), history (phone,channel)). `atlas migrate hash`+`validate` EXIT 0, `atlas.sum` régénéré. `atlas migrate lint` = SKIP (Pro tier depuis v0.38, à faire en CI `atlas login`). Pas d'architect-reviewer (SQL pur, 0 frontière Clean Arch). **Prérequis de T-012 levé.** Vigilance impl : upsert `ON CONFLICT (phone,channel)` + MAJ compteurs `contacts` en même transaction ; routing (T-016) accède à `contact_intel` via REST interne, jamais cross-schéma (D11). |
| T-012 | Contact Intelligence — domaine scoring joignabilité + ingestion historique + API REST interne | `src/contact-intelligence` | go-engineer | 🟡 | Done | PASS | Livré 2026-07-12 (Vague 1 V1, dep T-011). 27 fichiers, stdlib-only, go.mod/go.sum inchangés. C1 : `Contact`/`ChannelScore`/VO `DeliveryScore`/`Channel`/`DeliveryOutcome` + règle scoring **Laplace α=1** `score=floor((succ+1)/(succ+fail+2)*100)` (prior 50, bornée [0,100], déterministe, monotone). C2 : use cases `GetContactScore`/`EnrichRecipient`/`RecordDeliveryOutcome` + ports output (Contact/ChannelScore/History Repository + OutcomeStore). C3 : persistence Postgres (colonnes 0014 exactes), **`OutcomeStore.RecordAtomically` = upsert `ON CONFLICT (phone,channel)` + MAJ compteurs `contacts` + append historique dans UN seul `sql.Tx` (BEGIN/rollback différé/COMMIT)**, Broker en C3 + consumer DLR (`message.delivered`/`message.failed`→RecordDeliveryOutcome), HTTP `GET /contacts/{phone}/score` + `POST /outcomes`. C4 : config (port **8086**), postgres (`search_path=contact_intel`), rabbitmq (NoopBroker+assertion), httpserver + composition root `main.go` DI manuelle. **QA PASS 8/8** (`go build`/`vet ./src/...` exit 0 ; `go test ./src/contact-intelligence/...` **29 PASS** dont 10 handler ajoutés par QA ; 17 pkgs monorepo ok, 0 régression). **architect-reviewer CONFORME** (domaine pur, application ne dépend que des ports, 0 import adapters→infra, Broker C3, `sql.Tx` non fuité en C2/C1, isolation `contact_intel`, 0 cross-service). **Débloque T-016.** Contrat `GET score` (pour T-016) : `{phone, delivery_score, found, channel_scores[{channel,score,sample_size,last_success_at}]}`, inconnu → found=false + score 50 (pas 404), tri score DESC. Dettes/vigilances : (a) `success_count` par canal non persisté → reconstruit par inversion (±1 pt, candidat migration 0015) ; (b) parseur `text[]` manuel (→ `pq.Array` avec driver) ; (c) `EnrichRecipient` instancié mais non exposé (route/consumer à câbler) ; (d) **depguard/golangci absent du dépôt** (dette PRÉ-EXISTANTE, contredit CLAUDE.md/ARCHITECTURE.md §7 → à créer côté devops). **Dépend de** T-011. **Débloque** T-016 (routing `highest_delivery` enrichi). |
| T-013 | Schéma Provider — colonnes/seed canal Telegram (capabilities, credentials par workspace) | `migrations/` (schéma `provider`) | db-engineer | 🟡 | Done | PASS | `migrations/0015_provider_telegram.sql` (0014=dernier→0015). STRICTEMENT additive : `CREATE SCHEMA IF NOT EXISTS` + seed idempotent `INSERT INTO provider.providers (id,channel,enabled) VALUES ('telegram-bot','telegram',true) ON CONFLICT (id) DO NOTHING`. **Décision credentials (option a)** : réutilise le pattern existant `provider.provider_credentials.secret_enc bytea` (bot token = 1 secret global par provider, symétrique whatsapp-meta/sms-twilio ; ligne credentials insérée HORS migration, chiffrée AES-GCM — aucun secret en clair). Écartées : (b) env/Secret pur (rompt symétrie), (c) table par workspace (bot Telegram non multi-tenant à ce stade). **Décision capabilities** : PAS de colonne — capabilities = constantes Go côté adapter C3 (T-014), pas de requête SQL filtrant dessus. Id `telegram-bot` = convention `{channel}-{vendor}` (cf. `src/provider/main.go` registry). `channel='telegram'` accepté par `text` libre (0005), pas de CHECK. `atlas migrate hash`+`validate` EXIT 0, `atlas.sum` régénéré (0015 `h1:p99Dg+…`). `lint`=SKIP (Atlas Pro). Pas d'architect-reviewer (SQL pur, 0 frontière). **Débloque T-014.** Vigilances T-014 : câbler `"telegram-bot"` au registry `main.go`, lire token via `provider_credentials WHERE provider_id='telegram-bot'` (déchiffré AES-GCM), capabilities en constantes Go. Vigilance T-024 : provisionner le Secret K8s clé AES-GCM + insérer la ligne credentials chiffrée hors migration. **Prérequis de T-014.** |
| T-014 | Adapter provider Telegram (Bot API) + enregistrement dans le registry | `src/provider` | go-engineer | 🟡 | Done | PASS | Livré 2026-07-12 (Vague 2 V1, dep T-013). C3 `src/provider/internal/adapters/providers/telegram.go` (struct `TelegramBot`, assertion `var _ output.Provider`, stub `net/http` timeout 10s + `TODO(production)`, pattern WhatsAppMeta/SMSTwilio) + `telegram_test.go` (20 tests). **Mapping statut** `MapTelegramResponseStatus(ok, errorCode)` (fonction pure) : ok→Sent, 400/403→Rejected, ≥500/timeout(-1)/inconnu→Failed. **Coût** : `EstimateCost` toujours 0 (Telegram Bot API gratuit), devise cohérente (EUR FR/BE/CH, XAF défaut). **Capabilities** constantes Go (MaxMessageLength 4096, SupportsRichMedia, RequiresTemplate=false — pas de colonne base, D27). **Token via config/env** `TELEGRAM_BOT_TOKEN`/`TELEGRAM_BASE_URL` (défaut `https://api.telegram.org`), jamais loggé — **divergence assumée vs D27** (lecture future via `provider_credentials` AES-GCM = `TODO(production)`, car AUCUN adapter existant ne lit les credentials en base : ils passent tous par config/env ; dette tracée). Registry `main.go` : `"telegram-bot": telegramAdapter` (clé = `provider.providers.id` seedé 0015). **QA PASS 9/9** (`go build`/`vet ./src/...` exit 0, `go test ./src/provider/...` **64 PASS**, 18 pkgs monorepo ok/0 régression, go.mod inchangé, whatsapp/sms/domaine intacts). **architect-reviewer CONFORME** (adapter C3 importe domaine+stdlib seuls, 0 import infra, mapping pur, config C4, registry en main.go, 0 cross-service, capabilities confinées C3). Observation QA/archi (assertion inline) **soldée par PM** : remplacée par `var _ output.Provider = (*TelegramBot)(nil)` (import C3→C2 autorisé), re-build/vet/test verts. **Débloque T-015.** Vigilances : T-015 routing (channel `'telegram'`, fallback WhatsApp→SMS→Telegram) ; T-038 seed (`provider_pricing` cost=0 pour telegram-bot ; score délivrabilité défaut ~75-80 proposé, à trancher par db-engineer) ; DLR Telegram réel = webhook (pas de pull), adapter distinct ultérieur ; T-024 Secret K8s `TELEGRAM_BOT_TOKEN` + clé AES-GCM. **Dépend de** T-013. **Débloque** T-015. |
| T-015 | Routing — canal Telegram dans la sélection + fallback WhatsApp→SMS→Telegram | `src/routing` | go-engineer | 🟡 | Backlog | — | Ajouter `telegram` aux `Channel` du domaine routing, pricing/scores Telegram pris en compte par `SelectProvider` (4 stratégies), chaîne de fallback étendue (US-002, story API-02 : WhatsApp→SMS→Telegram). Le seed pricing/scores Telegram est traité séparément par T-038 (atomicité : 1 tâche = 1 agent). **Dépend de** T-014 **+ T-038** (seed). |
| T-016 | Routing — intégrer le score Contact Intelligence dans `highest_delivery` | `src/routing` | go-engineer | 🟡 | Done | PASS | Livré 2026-07-13 (Vague 2 V1, dep T-012). **Port C2** `ContactScoreClient.GetChannelScore(ctx, phone, channel) (score int, found bool, err error)` (types purs). **Client REST C3** `adapters/clients/contact_intel_client.go` (`net/http`+`encoding/json`, timeout 2s, URL `CONTACT_INTEL_API_URL`, parse contrat T-012, extrait score du canal sinon `delivery_score` global ; assertion `var _ output.ContactScoreClient` ; **n'importe JAMAIS `fleece/src/contact-intelligence`** — 2 occurrences = commentaires garde-fou, 0 import réel, isolation D11/D19). **Domaine pur** `CombineScore(providerScore, contactScore) = clamp((ps+cs+1)/2, 0,100)` (moyenne 50/50 round-half-up ; sémantique : le contact_score s'applique au **canal** → tous les providers d'un canal reçoivent le même delta, ordre relatif préservé ; canal peu joignable pour le destinataire = pénalisé). **Use case** : appelle le client UNIQUEMENT si `Strategy==highest_delivery && client!=nil && recipient!=""`, enrichit une COPIE de la slice scores, `SelectProvider` inchangé. **Fallback gracieux** (client nil / erreur / timeout / JSON invalide / recipient vide → AUCUNE erreur, dégradation vers provider_scores seuls, log warning). Signature `Execute` +`recipient string` ; DTO `RouteRequest.recipient` optionnel `omitempty` (rétrocompat). **QA PASS 9/9** (`go build`/`vet ./src/...` exit 0 ; `go test ./src/routing/...` domaine 20 + usecases 11 + http 19 PASS ; 18 pkgs monorepo ok/0 régression ; go.mod inchangé ; fallback testé nil/erreur/recipient-vide ; contact inconnu found=false→score 50). **architect-reviewer CONFORME** (port pur C2, client C3 sans import cross-service, domaine pur, use case dépend du port, câblage main.go C4→C3→C2, 0 SQL cross-schéma). Nitpicks (ligne morte main.go, gofmt) **soldés par le PM** (ligne retirée + `gofmt -w`, re-build/vet/test verts). **Débloque suite Vague 2.** **Dépend de** T-012. |
| T-017 | Schéma Campaign — migration additive (message, stratégie, métriques de run, statut par destinataire) | `migrations/` (schéma `campaign`) | db-engineer | 🟡 | Done | PASS | `migrations/0016_campaign_execution.sql` (0015=dernier→0016). STRICTEMENT additive (12 `ADD COLUMN IF NOT EXISTS`, 4 `CREATE [UNIQUE] INDEX IF NOT EXISTS` nommés, 0 destructif/UPDATE). `campaigns` +`message_body text ''`/`channel_strategy text 'lowest_cost'`/`estimated_cost bigint 0` (centimes, aligné wallet)/`currency text 'XAF'`/`rate_limit int 0` (0=illimité). `campaign_recipients` +`status text 'pending'`/`message_id uuid` (nullable, **colonne LIBRE non-FK** = corrélation DLR sans cross-schéma, D11) + **UNIQUE INDEX `uq_campaign_recipients_campaign_recipient (campaign_id, recipient)`** (dédup enforced base). `campaign_runs` +`total_count`/`sent_count`/`delivered_count`/`failed_count int 0`. Index lecture : `campaigns(workspace_id)`, `campaigns(status, scheduled_at)` (pattern scheduler `status='scheduled' AND scheduled_at<=now()`), `campaign_recipients(campaign_id, status)` (reprise de run). `atlas migrate hash`+`validate` EXIT 0, `atlas.sum` 0016 `h1:LFDi0Erh…`, `lint` SKIP Pro. Pas d'architect-reviewer (SQL pur, 0 frontière). **Débloque T-018.** Vigilances T-018 : machine à états (draft→scheduled[require message_body≠'' + scheduled_at]→running→completed/failed) ; dédup import `INSERT ... ON CONFLICT (campaign_id,recipient) DO NOTHING` ; estimation coût via client REST routing `provider_pricing` (jamais SQL cross-schéma) ; scheduler `UPDATE ... WHERE status='scheduled'` atomique anti-double-run ; rate_limit=token bucket/run ; consumer DLR corrèle par `message_id` (gérer NULL avant injection) ; reprise via `status='pending'`. **Prérequis de T-018.** |
| T-018 | Campaign — domaine (états, dédup, estimation coût) + planification + exécution via pipeline messaging | `src/campaign` | go-engineer | 🟡 | Backlog | — | C1 domaine (Campaign + machine à états draft/scheduled/running/completed/paused/cancelled, dédup destinataires, estimation coût) ; C2 use cases : `CreateCampaign`, `EstimateCost` (appelle wallet+routing), `ScheduleCampaign`, `RunCampaign` (décompose en messages individuels idempotents, respecte le rate limit workspace), `PauseCampaign`/`CancelCampaign`, `GetCampaignStatus` ; C3 persistence (schéma `campaign`, T-017) + clients REST internes (messaging POST envoi, wallet estimation/débit, routing) + consumer DLR (met à jour les métriques par destinataire) + http ; C4 scheduler (ticker → campagnes `scheduled` dues → RunCampaign) + composition root. Couvre US-004/US-005, story DASH-06 (back). Stdlib-only. **Dépend de** T-017 ; **réutilise** messaging (T-001) + wallet (T-002) + routing. |
| T-019 | Schéma Analytics — vues matérialisées + agrégats KPI (délivrabilité, coût, latence par pays/canal) | `migrations/` (schéma `analytics`) | db-engineer | 🟡 | Done | PASS | `migrations/0017_analytics_kpi.sql` (0016=dernier→0017). STRICTEMENT additive (schéma `analytics` seul, 0009 intact, 0 FK/requête cross-schéma D11). `message_daily` +`delivery_latency_ms_sum bigint 0` (**latence en SOMME additive** — moyenne calculée à la lecture, évite la non-additivité d'une moyenne stockée). **MV `analytics.kpi_daily`** (`CREATE MATERIALIZED VIEW IF NOT EXISTS ... WITH NO DATA`) exposant day/workspace_id/country/channel/sent/delivered/failed/cost + KPIs `delivery_rate` (delivered/sent), `failure_rate` (failed/sent), `avg_cost_per_msg` (cost/sent), `avg_delivery_ms` (latency_sum/delivered) — protection division/0 via `CASE WHEN dénom>0` (0, sauf avg_delivery_ms→NULL). Requête MV = `FROM analytics.message_daily` seul (D11). 3 index perf (`(workspace_id,day)`, `(workspace_id,country)`, `(workspace_id,channel)`) + **index UNIQUE `uq_kpi_daily_dimensions (day,workspace_id,country,channel)`** requis pour `REFRESH ... CONCURRENTLY`. Refresh : 1er non-concurrent (MV vide) puis périodique CONCURRENTLY (T-020). **QA PASS 8/8** (numéro libre, 100% additif, 0009 intact, isolation/D11 vérifiée MV mono-schéma, 4 KPIs + protection div/0, index UNIQUE présent, atlas hash/validate EXIT 0, style). Observation QA (commentaire NULLIF vs DDL CASE WHEN) **soldée par PM** (commentaire aligné + `atlas migrate hash`/`validate` re-EXIT 0, `atlas.sum` 0017 `h1:XDsGqEyE…`). `lint` SKIP Pro/Docker. Pas d'architect-reviewer (SQL pur). **Débloque T-020.** Vigilances T-020 : consumer `message.delivered`/`message.failed` UPSERT `message_daily` (`ON CONFLICT (day,workspace_id,country,channel) DO UPDATE`, incrémente sent/delivered/failed/cost/latency_sum ; définir clairement quel événement incrémente `sent` pour éviter double-comptage) ; job refresh (1er non-concurrent puis CONCURRENTLY, timeout ~30s, `ANALYTICS_REFRESH_INTERVAL` défaut 5min, couche 4) ; use cases GetKpis/GetTimeSeries lisent `kpi_daily` ; `search_path=analytics` strict. **Prérequis de T-020.** |
| T-020 | Analytics — ingestion d'événements + use cases KPI + API REST interne | `src/analytics` | go-engineer | 🟡 | Backlog | — | C1 domaine (KPI VO, agrégats) ; C2 use cases : `IngestMessageEvent` (consomme `message.sent/delivered/failed` + coût → incrémente `message_daily`), `GetKpis` (filtres période/pays/canal), `GetTimeSeries` ; C3 persistence (schéma `analytics`, vues T-019) + consumer RabbitMQ (événements messaging/wallet) + interface Broker C3 + http (GET /kpis, GET /timeseries) ; C4 config/postgres/rabbitmq/httpserver + job de refresh des vues + composition root. Couvre KPIs PRD §14, story DASH-05 (back). Stdlib-only. **Dépend de** T-019. |
| T-021 | BFF GraphQL — exposer Analytics + Campaigns (schéma SDL, résolveurs, clients REST) | `src/graphql-api` | ts-engineer | 🟡 | Backlog | — | SDL : types `Kpi`/`TimeSeriesPoint`/`Campaign`/`CampaignRun` + Query `analytics(workspaceId, filters)`/`campaigns(workspaceId)`/`campaign(id)` + Mutation `createCampaign`/`scheduleCampaign`/`pauseCampaign`/`cancelCampaign`. C2 ports clients `AnalyticsClient`/`CampaignClient` + use cases d'agrégation ; C3 résolveurs + clients REST (analytics T-020, campaign T-018). Solder en passant les dettes T-007.4 (`Query.transactions`) si pertinent pour DASH-05. **Dépend de** T-018 + T-020. **Débloque** T-022/T-023. |
| T-022 | Dashboard — écran Analytics (KPIs, graphiques temporels, filtres période/pays/canal) | `src/platform-app` | frontend-engineer | 🟡 | Backlog | — | Story DASH-05. Section « Analytics » : cartes KPI, charts temporels, tableaux de répartition, filtres ; états vide/erreur/retry ; données via `lib/graphql/` → BFF (T-021), 0 import backend. Bibliothèque de charts à intégrer (shadcn + charts). WCAG 2.1 AA (alternatives tabulaires). **Dépend de** T-021. |
| T-023 | Dashboard — écran Campaigns (assistant multi-étapes, estimation coût, suivi en direct) | `src/platform-app` | frontend-engineer | 🟡 | Backlog | — | Story DASH-06. Section « Campaigns » : stepper import destinataires → rédaction → stratégie canal → estimation coût temps réel → planifier/lancer → suivi ; avertissement solde insuffisant ; statut campagne + métriques par destinataire ; pause/annulation. Données via BFF (T-021). **Dépend de** T-021. |
| T-024 | DevOps — manifests K8s contact-intelligence/campaign/analytics + CI + ConfigMap URLs internes | `deploy/k8s/`, `.github/` | devops-engineer | 🟡 | Backlog | — | Deployment+Service+HPA pour les 3 nouveaux services Go (noms DNS = URLs internes), env POSTGRES_DSN/SEARCH_PATH par schéma (`contact_intel`/`campaign`/`analytics`)/RABBITMQ_URL, ajout aux ConfigMap URLs (CONTACT_INTEL_API_URL/CAMPAIGN_API_URL/ANALYTICS_API_URL pour BFF + routing). Jobs/CI : ajouter au workflow Go (déjà couvert par `go build ./src/...`) ; solder la dette T-009.2 (`make build pkg=rest-api`) si rest-api implémenté. **Transverse V1**, à séquencer après T-012/T-018/T-020. |
| T-025 | QA — suite d'acceptance V1 (Contact Intelligence, Telegram, Campagnes, Analytics) | transverse | qa-engineer | 🟡 | Backlog | — | Stories US-002/004/005, DASH-05/06 + scoring joignabilité + canal Telegram + fallback étendu. Même modèle que T-010 (offline-safe : `go test ./src/...`, SDL↔front, routes↔use cases) + tests d'intégration ciblés (scoring, dédup campagne, agrégats KPI). Verdict PASS/FAIL global. **Jalon de clôture V1.** |

### Backlog V2 (🔵 P2) — SSO, Messenger, RCS, optimisation IA du routage (~6 mois)

| ID | Tâche | Service / zone | Agent | Phase | Statut | Acceptance | Notes |
|----|-------|----------------|-------|-------|--------|-----------|-------|
| T-026 | Identity — SSO (Better Auth SSO/OIDC provider) | `src/auth-api` | ts-engineer | 🔵 | Backlog | — | Activer le plugin SSO de Better Auth (OIDC/SAML) confiné `adapters/auth/` ; domaine : association compte SSO ↔ workspace, provisioning JIT, mapping rôles ; use cases C2 (initiate/callback SSO → ApiContext session) ; repository `SsoConnectionRepository` (port C2 + impl Drizzle C3 sur la table de T-039). Suit le follow-up DASH-02 §«rôles/permissions, SSO». **Critères** : `tsc --noEmit` exit 0 ; better-auth confiné C3/C4 ; ports C2 à types purs (pas de fuite better-auth) ; SSO callback → `ApiContext {authMethod:"session"}`. **Dépend de** T-039 (schéma `sso_connections`). S'appuie sur T-006 (Identity MVP). |
| T-027 | Dashboard — login SSO + gestion des connexions SSO (admin workspace) | `src/platform-app` | frontend-engineer | 🔵 | Backlog | — | Écran login « Se connecter via SSO » (same-origin `/api/auth/sso/*`) + écran admin de configuration des connexions SSO du workspace. Données via BFF/routes auth. **Dépend de** T-026. |
| T-028 | Schéma Provider — canaux Messenger + RCS (capabilities, credentials) | `migrations/` (schéma `provider`) | db-engineer | 🔵 | Backlog | — | Migration additive `provider` : canaux `messenger`/`rcs`, capabilities (rich media, templates), credentials. **Prérequis de** T-029/T-030. |
| T-029 | Adapter provider Meta Messenger (Send API) | `src/provider` | go-engineer | 🔵 | Backlog | — | C3 `adapters/providers/messenger.go` implémentant le port `Provider` (Send API Meta), mapping statut → DeliveryStatus, enregistrement registry. Même pattern que Telegram (T-014). **Dépend de** T-028. **Prérequis V1 :** pattern adapter validé par T-014 (Telegram). |
| T-030 | Adapter provider RCS (RBM / agrégateur) | `src/provider` | go-engineer | 🔵 | Backlog | — | C3 `adapters/providers/rcs.go` implémentant le port `Provider` (RCS Business Messaging ou agrégateur), capabilities rich media, mapping statut. **Dépend de** T-028. **Prérequis V1 :** pattern adapter Telegram (T-014). |
| T-031 | Routing — Messenger + RCS dans la sélection et le fallback étendu | `src/routing` | go-engineer | 🔵 | Backlog | — | Ajouter `messenger`/`rcs` aux `Channel` du domaine routing, pricing/scores pris en compte par `SelectProvider` (4 stratégies), chaîne de fallback étendue (WhatsApp→RCS→SMS→Telegram→Messenger selon stratégie). Le seed pricing/scores est traité par T-040 (atomicité). **Critères** : `go test ./src/routing/...` PASS, go.mod inchangé, isolation schéma `routing`. **Dépend de** T-029 + T-030 **+ T-040** (seed) ; **réutilise** le pattern T-015. |
| T-032 | Routing IA — collecte de features + scoring ML de sélection de provider | `src/routing` | go-engineer | 🔵 | Backlog | — | C1/C2 : port `ProviderScorer` (interface) avec implémentation ML (modèle entraîné sur l'historique délivrabilité/latence/coût de Contact Intelligence + Analytics) ; feature store / inférence en C3 (`adapters/`), modèle = détail d'infrastructure. Fallback déterministe (stratégies V0) si l'inférence échoue. **Prérequis V1 critiques :** T-012 (Contact Intelligence, données de joignabilité) + T-020 (Analytics, historique KPI) — sources de features. |
| T-033 | Routing IA — A/B testing de stratégies (assignation de variante) | `src/routing` | go-engineer | 🔵 | Backlog | — | Scindé (côté routing seul ; collecte analytics + feedback = T-042 ; migrations = T-041). C1 domaine : `Experiment`/`Variant` (assignation déterministe d'une variante de stratégie par workspace/expérience, split de trafic) ; C2 use case `AssignStrategyVariant` (entrée du routage : choisit la variante active) + persistance expériences (schéma `routing`, table T-041). Émet un événement `routing.variant.assigned` (port output) consommé par Analytics (T-042). **Critères** : `go test ./src/routing/...` PASS, assignation déterministe/reproductible, isolation schéma `routing`. **Dépend de** T-032 + T-041. |
| T-034 | Dashboard V2 — Messenger/RCS dans les écrans + visualisation A/B routage + analytics enrichies | `src/platform-app` | frontend-engineer | 🔵 | Backlog | — | Afficher les nouveaux canaux (Messenger/RCS) dans campagnes/analytics, écran de suivi des expériences A/B de routage et des recommandations IA, analytics enrichies (par variante de stratégie). Données via BFF (T-035). **Dépend de** T-035. |
| T-035 | BFF GraphQL — exposer SSO connections + canaux V2 + expériences A/B routage | `src/graphql-api` | ts-engineer | 🔵 | Backlog | — | SDL/résolveurs/clients pour : connexions SSO (config workspace), canaux Messenger/RCS dans campaigns/analytics, expériences A/B de routage (lecture + métriques par variante). Clients REST vers auth-api (SSO) + routing (expériences, T-033) + analytics (métriques variantes, T-042). **Critères** : SDL↔résolveurs alignés (0 fantôme), `tsc --noEmit` exit 0, application/ sans framework, clients C3 `_conformance`. **Dépend de** T-026 + T-031 + T-033 + T-042. **Débloque** T-034. |
| T-036 | DevOps — infra ML (inférence/feature store), SSO secrets, CI canaux V2 | `deploy/k8s/`, `.github/` | devops-engineer | 🔵 | Backlog | — | Manifests pour le composant d'inférence ML (sidecar/service ou job d'entraînement), Secrets SSO (OIDC client secrets) + Messenger/RCS credentials, mise à jour ConfigMap. CI : tests des nouveaux adapters. **Transverse V2**, à séquencer avec T-032/T-033. |
| T-037 | QA — suite d'acceptance V2 (SSO, Messenger, RCS, routage IA + A/B, API REST publique) | transverse | qa-engineer | 🔵 | Backlog | — | Stories follow-up DASH-02 (SSO), canaux Messenger/RCS dans le fallback, scoring IA (non-régression vs stratégies déterministes), A/B testing (assignation/feedback), **+ API REST publique (rest-api) : stories API-01..04 via le vrai gateway public (API Key auth, rate limiting, envoi/wallet/webhooks)**. Même modèle que T-010/T-025 + tests dédiés au scoring ML (déterminisme du fallback, isolation du modèle en C3) + rate limiting (429). **Jalon de clôture V2.** |

### Ajouts V1 (🟡 P1) — scission d'atomicité (2026-07-11)

| ID | Tâche | Service / zone | Agent | Phase | Statut | Acceptance | Notes |
|----|-------|----------------|-------|-------|--------|-----------|-------|
| T-038 | Routing — migration additive seed pricing + scores canal Telegram | `migrations/` (schéma `routing`) | db-engineer | 🟡 | Backlog | — | Scindé de T-015 (atomicité : la migration de seed est un livrable db-engineer distinct du code Go de sélection). Migration additive `00NN` (`INSERT ... ON CONFLICT DO NOTHING` idempotent, `CREATE ... IF NOT EXISTS` si nouvelle table) : lignes `routing.provider_pricing`/`routing.provider_scores` pour le canal `telegram` (coût, delivery/latence par défaut, `country` si pertinent). Un seul schéma `routing`, pattern dette schéma (jamais modifier 0004/0013). **Dépend de** T-013 (schéma provider Telegram) + T-014 (adapter, pour disposer du/des providerId Telegram). **Débloque** T-015. |

### Ajouts V2 (🔵 P2) — scissions d'atomicité + track API REST publique (2026-07-11)

| ID | Tâche | Service / zone | Agent | Phase | Statut | Acceptance | Notes |
|----|-------|----------------|-------|-------|--------|-----------|-------|
| T-039 | Schéma Identity — migration additive `sso_connections` (SSO par workspace) | `migrations/` (schéma `identity`) | db-engineer | 🔵 | Backlog | — | Scindé de T-026 (schéma avant service, pattern V1). Migration additive `00NN` : table `identity.sso_connections` (workspace_id, provider, domain, config JSONB, enabled, created_at) + index sur `workspace_id`/`domain`. Un seul schéma `identity`, pattern dette schéma (jamais modifier 0001/0011). **Prérequis de** T-026. |
| T-040 | Routing — migration additive seed pricing + scores Messenger + RCS | `migrations/` (schéma `routing`) | db-engineer | 🔵 | Backlog | — | Scindé de T-031 (miroir de T-038 pour Telegram). Migration additive `00NN` idempotente (`INSERT ... ON CONFLICT DO NOTHING`) : lignes `routing.provider_pricing`/`routing.provider_scores` pour `messenger` et `rcs`. Un seul schéma `routing`. **Dépend de** T-028 (schéma provider Messenger/RCS) + T-029/T-030 (adapters, pour les providerId). **Débloque** T-031. |
| T-041 | Schéma A/B routage — migrations additives `routing.experiments` + métriques par variante `analytics` | `migrations/` (schémas `routing` ET `analytics`) | db-engineer | 🔵 | Backlog | — | Scindé de T-033. **DEUX fichiers** (règle un-schéma-par-fichier) : (a) `00NN_routing_experiments.sql` — `routing.experiments` (id, workspace_id, name, variants JSONB, traffic_split, status, created_at) ; (b) `00NN+1_analytics_variant_metrics.sql` — table/vue `analytics.experiment_variant_metrics` (experiment_id, variant, delivered/failed/cost agrégés). Additifs, patterns dette schéma. **Prérequis de** T-033 (routing) + T-042 (analytics). |
| T-042 | Analytics — agrégation métriques par variante A/B + boucle de feedback | `src/analytics` | go-engineer | 🔵 | Backlog | — | Scindé de T-033 (côté analytics). C2 use case : consumer `routing.variant.assigned` + corrélation avec `message.delivered/failed`+coût → agrège les métriques par variante (table T-041b) ; expose `GetVariantMetrics` (HTTP interne) pour la boucle de feedback réentraînant le scoring T-032. C3 persistence (schéma `analytics`) + consumer RabbitMQ + http. **Critères** : `go test ./src/analytics/...` PASS, isolation schéma `analytics`, stdlib-only. **Dépend de** T-041 + T-020 + T-033. **Débloque** T-035. |
| T-043 | API REST publique — implémenter le gateway `rest-api` (D22/D25) : auth API Key + routes publiques + clients REST | `src/rest-api` | ts-engineer | 🔵 | Backlog | — | **Décision D25 : rest-api sort en V2** (dette MVP API-first, surface publique développeurs externes). Implémenter les couches actuellement scaffolées : C3 `adapters/http/` (routes publiques : POST envoi message, GET statut, wallet solde/historique, api-keys lecture, webhooks CRUD) + `adapters/clients/` (clients REST → messaging/routing/wallet/webhook/auth) ; C4 `infrastructure/server.ts` (Hono/Express, monte le middleware API Key partagé de `@fleece/api-common`, écoute `PORT`). Auth = API Key (→ auth-api `ValidateApiKey`, T-006). Application + adapters uniquement (pas de règle métier, symétrique du BFF). **Critères** : `make build pkg=rest-api` (tsc+esbuild), API Key middleware branché, 401 sans clé, clients `_conformance`, application/ sans framework. **Dépend de** T-006 (ValidateApiKey) + messaging/wallet/webhook (MVP). **Débloque** T-044/T-045. |
| T-044 | API REST publique — rate limiting Redis + quotas (req/min, msg/min) | `src/rest-api` | ts-engineer | 🔵 | Backlog | — | Middleware de rate limiting adossé à Redis (PRD §Rate Limiting + §Anti-fraude) : quotas configurables par workspace/clé (requêtes/min et messages/min), réponse `429` + en-têtes `Retry-After`/`X-RateLimit-*`. Le client Redis est un détail C4 (infrastructure) injecté ; le middleware s'insère dans `adapters/http/` via un port. **Critères** : `tsc --noEmit` exit 0, 429 au dépassement, Redis confiné C4. **Dépend de** T-043. |
| T-045 | DevOps — déploiement K8s `rest-api` + Redis (rate limiting) + Secret API + CI | `deploy/k8s/`, `.github/` | devops-engineer | 🔵 | Backlog | — | Manifests manquants : Deployment+Service+HPA `rest-api` (nom DNS, port, env URLs internes des services Go + `REDIS_URL` + `AUTH_API_URL`), infra Redis (Deployment/Service ou statefulset dev), Secret API Key salt/pepper si requis. CI : le build `make build pkg=rest-api` est déjà ajouté (dette T-009.2 soldée) → ajouter l'image `make image pkg=rest-api` au pipeline si pertinent. **Dépend de** T-043 (+ T-044 pour Redis). |

> **Convention de découpe** : pour chaque nouveau service/canal, l'ordre est `db-engineer (schéma) → go/ts-engineer (impl) → qa-engineer → architect-reviewer`, puis `devops-engineer` (manifests/CI) une fois le service implémenté. Les migrations de schéma suivent le **pattern dette schéma** validé en MVP (migration additive `00NN`, `ADD COLUMN IF NOT EXISTS`, jamais modifier une migration existante, un seul schéma par fichier).

### Ordre d'exécution recommandé — V1 🟡 (vagues séquentielles, respect des dépendances)

> Chaque vague est parallélisable en interne ; on ne démarre une vague que lorsque ses prérequis (vagues précédentes) sont Done/PASS. À dérouler une tâche à la fois lors des prochaines sessions.

- **Vague 0 (fait)** : **T-011** ✅ (schéma Contact Intelligence, Done/PASS 2026-07-05).
- **Vague 1 — schémas & 1er service** (parallèle) : **T-012** (Contact Intel service, dep T-011) · **T-013** (schéma provider Telegram) · **T-017** (schéma Campaign) · **T-019** (schéma Analytics).
- **Vague 2 — services & adapters** (parallèle) : **T-014** (adapter Telegram, dep T-013) · **T-016** (routing highest_delivery + score Contact Intel, dep T-012) · **T-018** (service Campaign, dep T-017) · **T-020** (service Analytics, dep T-019).
- **Vague 3 — agrégation & seed** (parallèle) : **T-038** (seed pricing/scores Telegram, dep T-013+T-014) · **T-021** (BFF Analytics+Campaigns, dep T-018+T-020) · **T-024** (DevOps manifests contact-intel/campaign/analytics + CI, après T-012/T-018/T-020 ; étendre au Secret bot-token Telegram + env provider).
- **Vague 4 — routing Telegram & dashboards** (parallèle) : **T-015** (routing Telegram + fallback, dep T-014+T-038) · **T-022** (dashboard Analytics DASH-05, dep T-021) · **T-023** (dashboard Campaigns DASH-06, dep T-021).
- **Vague 5 — jalon** : **T-025** (QA suite d'acceptance V1, après toutes les vagues) → clôture V1.

**Observations backlog V1 (non bloquantes)** :
1. **Contact Intelligence sans écran dashboard** : T-012/T-016 sont un enrichissement *backend* (scoring pour routing). Aucune story dashboard V1 dédiée aux contacts n'est prévue au scope — assumé, à confirmer avec le PRD/user-story si un écran « joignabilité » devient souhaité (candidat V2).
2. **Gateway REST public `src/rest-api` (D22)** : ~~à planifier séparément~~ **TRANCHÉ le 2026-07-11 (D25) : sort en V2** → track dédié T-043 (impl) + T-044 (rate limiting Redis) + T-045 (DevOps). Voir section V2.
3. **T-024** couvre les 3 nouveaux services Go ; penser à y inclure le **Secret bot-token Telegram** et les env provider (le canal Telegram tourne dans le service `provider` existant, pas de nouveau Deployment).

### Ordre d'exécution recommandé — V2 🔵 (vagues séquentielles, respect des dépendances)

> Prérequis global : **V1 doit être Done/PASS** pour les tâches d'optimisation IA (T-032/T-033/T-042 consomment Contact Intelligence T-012 + Analytics T-020). Les tracks **SSO**, **canaux Messenger/RCS** et **API REST publique** ne dépendent que du MVP et peuvent démarrer sans attendre V1. Chaque vague est parallélisable en interne.

- **Vague A — schémas & fondations** (parallèle) : **T-039** (schéma `sso_connections`) · **T-028** (schéma provider Messenger/RCS) · **T-043** (impl gateway `rest-api`, ne dépend que du MVP).
- **Vague B — services, adapters & IA** (parallèle) : **T-026** (SSO Identity, dep T-039) · **T-029** (adapter Messenger, dep T-028) · **T-030** (adapter RCS, dep T-028) · **T-044** (rate limiting Redis, dep T-043) · **T-032** (scoring ML routage, dep V1 T-012+T-020).
- **Vague C — seeds, schéma A/B, dashboard SSO, DevOps rest-api** (parallèle) : **T-040** (seed Messenger/RCS, dep T-029+T-030) · **T-041** (schémas A/B routing+analytics, dep T-032) · **T-027** (dashboard login/admin SSO, dep T-026) · **T-045** (DevOps rest-api + Redis, dep T-043/T-044).
- **Vague D — routing V2** (parallèle) : **T-031** (routing Messenger/RCS + fallback, dep T-029+T-030+T-040) · **T-033** (routing A/B assignation, dep T-032+T-041).
- **Vague E — analytics variantes & infra ML** (parallèle) : **T-042** (analytics métriques par variante + feedback, dep T-041+T-020+T-033) · **T-036** (DevOps infra ML/inférence + Secrets SSO + credentials Messenger/RCS + CI, après T-032/T-033).
- **Vague F — BFF V2** : **T-035** (SDL/résolveurs/clients SSO + canaux + A/B, dep T-026+T-031+T-033+T-042).
- **Vague G — dashboard V2** : **T-034** (Messenger/RCS + visualisation A/B + analytics enrichies, dep T-035).
- **Vague H — jalon** : **T-037** (QA acceptance V2 incluant l'API REST publique, après toutes les vagues) → clôture V2.

**Observations backlog V2 (non bloquantes)** :
1. **Campaign / Analytics / Webhook absorbent Messenger/RCS automatiquement** : le canal est une *dimension* (routing sélectionne, campaign décompose en messages, analytics agrège par canal, webhooks sont channel-agnostic). Pas de tâche backend V2 dédiée pour ces services — seule l'UI (T-034) et le routing (T-031) changent. Assumé.
2. **Email hors scope** : cité dans le contexte marché (PRD §2) mais absent de toute phase de la roadmap (MVP/V1/V2) — non planifié.
3. **A/B testing** : le PRD le mentionne en follow-up *campagnes* (user-story), mais l'interprétation retenue (cohérente avec « optimisation IA du routage » V2) le place côté **routage** (T-033/T-041/T-042). Un A/B *de contenu de campagne* resterait un candidat ultérieur distinct.
4. **Décision D25** : rest-api sort en V2 comme track indépendant (non bloquant pour SSO/canaux/IA) ; il conditionne l'exposition publique des nouveaux canaux aux développeurs externes.

---

## Journal d'acceptance

### Remboursement dettes
| Date | Dette | Verdict | Détails |
|------|-------|---------|---------|
| 2026-07-11 | **Remboursement de dette transverse (D-E01/02/03/06, T-007.4, T-008.1a/c/f, T-008.4, T-009.2/.4, harmonisation `@anthill/`)** — branche `chore/tech-debt-paydown` | **PASS / CONFORME** | Lot transverse orchestré par le PM : go-engineer (wallet) ‖ devops → ts-engineer (BFF+auth-api) → frontend-engineer (‖ finalisation après stall) → qa + architect-reviewer. **1. Wallet (Go)** — **D-E01/T-007.4** : use case C2 `ListTransactions` (`src/wallet/internal/application/usecases/list_transactions.go` + test) délègue au port existant `TransactionRepository.ListByWorkspace` ; endpoint C3 `GET /wallets/{workspaceId}/transactions` (`adapters/http/handler.go` route `r.PathValue`, `dto.go` +`created_at`) ; câblage C4 `main.go`. Stdlib-only, go.mod inchangé, 3 tests (non-vide/vide-non-nil/erreur repo). **2. BFF graphql-api (TS)** : SDL `schema.graphql` +`Query.transactions`/`Mutation.rotateApiKey` (**D-E03/T-008.1a**)/`Mutation.createWorkspace` (**T-008.1f**)/`WebhookEndpoint.secretPreview` (**D-E02/T-008.1c**, secret masqué `••••<4 derniers>`, jamais exposé en clair) + résolveurs mappés + ports (`clients.ts`) + clients (`identity.rotateApiKey`/`createWorkspace`, `wallet.listTransactions`→GET wallet + getBalance pour la devise + pagination cursor BFF) avec `_conformance`. Bugfix TS préexistant `auth.handler.ts` (`Buffer.concat` variance TS 5.9+). Ajout dépendance explicite `@fleece/api-common` à `graphql-api/package.json` (observation architecte). **3. Frontend platform-app** — **D-E06/T-008.4** : `lib/session.ts` (`resolveSession(cookieHeader)`→auth-api `/validate-session` par fetch, fallback `DEV_WORKSPACE_ID`) + `lib/context/WorkspaceContext.tsx` (`useWorkspaceId`) ; layout résout la session côté serveur ; pages api-keys/wallet/webhooks/messages migrées (plus de constante `WORKSPACE_ID_FROM_SESSION` exécutable) ; rotation atomique (`rotateApiKey`), historique wallet réel (`transactions`), secret via `secretPreview`, onboarding appelle réellement `createWorkspace`. **4. Harmonisation** `@anthill/`→`@fleece/` sur auth-api, graphql-api et les 5 libs `src/ts/*` (0 import à corriger, grep `@anthill` = 0). **5. DevOps** — **T-009.2** : CI `make build pkg=rest-api` ajouté au job ts ; **T-009.4** : cible Makefile `k8s-configmaps` idempotente (dry-run|apply) + README/atlas-migrate.yaml mis à jour. **QA PASS 6/6 sections** : `go build`/`vet` exit 0, `go test ./src/... -count=1` **tous PASS (15 pkgs)**, go.mod inchangé ; SDL↔résolveurs 13/13 alignés (0 fantôme) ; `tsc --noEmit` exit 0 (graphql-api, auth-api, platform-app) ; `@anthill`=0 ; `WORKSPACE_ID_FROM_SESSION` = 0 constante exécutable (2 commentaires JSDoc tolérés) ; ci.yml/Makefile parse OK. **architect-reviewer CONFORME** : wallet use case C2 ne dépend que du port, handler C3 délègue, câblage C4 ; BFF application/ sans framework, clients C3 implémentent ports C2 (inversion+_conformance), graphql-yoga confiné C4, secret non fuité ; frontend 0 import backend, données 100% via BFF ; isolation schéma `wallet`. **Dettes résiduelles NON soldables offline (réseau)** : clients REST BFF encore stubs (fetch réel = `TODO(production)`, D-E05/T-007.1/.2), Apollo/Yoga non installés, `createWorkspace` auth-api attend `ownerEmail` (à dériver du token de session en prod), `WebhookEndpoint.secret` à exposer côté service webhook Go, top-up wallet réel = V2 (paiement Mobile Money/Stripe, D8), runtime `npm install`/`next build`/`docker`/`kubectl`/`atlas apply` à valider en CI. |
| 2026-06-19 | **D1 ValidateSession workspaceId** (auth-api + graphql-api) — CRITIQUE | **PASS / CONFORME** | Séquence ts-engineer → qa (FAIL 1er tour) → ts-engineer (correctif) → re-PASS ; architect-reviewer CONFORME. **auth-api** : nouveau use case C2 `application/use-cases/validate-session.ts` — `ValidateSession.execute(token)` appelle `authProvider.validateSession(token)` → `userRepository.findById(userId)` → retourne `ApiContext {workspaceId: user.workspaceId, userId, authMethod:"session"}` (l'entité `User` porte directement `workspaceId`, résolution O(1) sans WorkspaceRepository). Lève `UnauthorizedError` si session invalide ou user introuvable. Pas de port input séparé (calque le pattern ValidateApiKey/CreateApiKey). `adapters/http/auth.handler.ts` : `POST /validate-session` délègue ENTIÈREMENT au use case (Bearer prioritaire + fallback body `{token}`), plus aucune logique de résolution dans le handler, 401 sur Unauthorized. `index.ts` câble `new ValidateSession(authProvider, userRepo)`. **graphql-api** : `infrastructure/session-validator.ts` — `fetch(${AUTH_API_URL}/validate-session)` ACTIVÉ (décommenté), réponse typée `ValidateSessionResponse`, fallback `null` si non-OK/catch, assertion `_conformance` au port `SessionValidator`. **QA 1er tour FAIL** : `errorToStatus` (mappeur global) mappait `UnauthorizedError`→403/FORBIDDEN alors que la sémantique HTTP exige 401 (chemin nominal OK via try/catch interne, mais bug latent). **Correctif ts-engineer** : `errorToStatus` → `UnauthorizedError` = 401/UNAUTHORIZED ; vérifié qu'aucune autre route ne régresse (InvalidApiKey reste 401, ApiKeyRevoked reste 400, validate-key ne lève pas UnauthorizedError). **architect-reviewer CONFORME** : use case C2 n'importe que domain + @fleece/api-common + ports (0 framework), port `AuthProvider.validateSession` retourne `SessionData` types purs (pas de fuite better-auth), handler C3 délègue au use case, HttpSessionValidator reste C4 + implémente port SessionValidator, Drizzle/better-auth confinés C3/C4, composition root C4→C3→C2. **Offline** : aucun npm install, better-auth/drizzle 100% stubés/commentés, tsc système absent → type-check SKIP documenté + revue statique. 4 tests unitaires ajoutés (`__tests__/validate-session.test.ts`). **Dette résiduelle SOLDÉE** : `/validate-session` retourne désormais un `workspaceId` réel ; reste runtime (Apollo/Yoga à activer pour servir le BFF). |
| 2026-06-19 | **D2 Provider DLR status non persisté** (provider) — IMPORTANT | **PASS / CONFORME** | Séquence db-engineer (0012) → go-engineer → qa + architect-reviewer (parallèle). **db-engineer** : `migrations/0012_provider_dlr_status.sql` créée (0011 dernier pris → 0012), additive (`ADD COLUMN IF NOT EXISTS status VARCHAR(50) NOT NULL DEFAULT 'pending'`, `updated_at TIMESTAMPTZ`). `external_id` NON ajoutée car déjà présente depuis 0005 (`text NOT NULL`). 0 modif de 0005, schéma `provider` uniquement. **go-engineer** : `adapters/persistence/record.go` (`providerMessageRecord` + `Status string`, `UpdatedAt *time.Time`) ; `provider_repository.go` (Save persiste `status='pending'`+`external_id`, ON CONFLICT préserve le statut existant ; nouvelle méthode `UpdateStatus(ctx, externalId string, status string, updatedAt time.Time) error` → `UPDATE ... WHERE external_id = $1` car le DLR ne fournit que l'externalId ; assertion `var _ output.ProviderRepository`) ; `ports/output/ports.go` (port `UpdateStatus` en C2) ; `usecases/handle_dlr.go` (appelle `UpdateStatus` APRÈS publication événement ; mapping `StatusDelivered`→"delivered", `StatusFailed`→"failed", `StatusRejected`→"rejected" ; TODO dette retiré) ; `main.go` (câblage inchangé). **QA** : `go build ./src/...` OK (0 régression 15 pkgs), `go vet ./src/provider/...` OK, `go test ./src/provider/...` PASS (+4 tests handle_dlr PersistsStatus delivered/failed/rejected + NilRepo no-panic), go.mod inchangé/go.sum absent, 0 import tiers. **architect-reviewer CONFORME** : port C2/impl C3 (inversion), assertion présente, 0 import adapters→infra, domaine inchangé/pur, Broker reste C3, isolation schéma `provider`. **Stdlib-only.** |
| 2026-06-19 | **D3 Routing Fastest/Custom non fonctionnels** (routing) — MOYEN | **PASS / CONFORME** | Séquence db-engineer (0013) → go-engineer → qa + architect-reviewer (parallèle). **db-engineer** : `migrations/0013_routing_strategies.sql` additive — `routing.provider_scores` (+`avg_latency_ms INTEGER NOT NULL DEFAULT 0`, `country VARCHAR(10) NOT NULL DEFAULT ''`), `routing.routing_rules` (+`channel VARCHAR(50) NOT NULL DEFAULT ''`, `config JSONB` nullable). 0 modif de 0004. **go-engineer** : domaine — `ProviderScore` +`AvgLatencyMs int`/`Country string`, `RoutingRule` +`Channel string`/`Config RoutingConfig` (`RoutingConfig = map[string]any`, type PUR sans I/O). `selector.go` : **Fastest** = tri `AvgLatencyMs` ASC, tie-break documenté (score desc → coût asc → ordre lexico ; latences toutes à 0 ⇒ comportement = HighestDelivery, repli safe) ; **Custom** = applique `Config["preferred_order"]` (tableau de provider IDs en tête dans l'ordre donné, absents en queue triés par score), fallback `HighestDelivery` si Config nil/clé absente/vide ; TODO retirés. **Changement signature** `SelectProvider(candidates, scores, rule RoutingRule)` (au lieu de `strategy`) pour accéder à `rule.Config` ; `get_routing_decision.go` passe la `RoutingRule` complète chargée (ou défaut `{Strategy: HighestDelivery}`). Décodage JSONB confiné à `adapters/persistence/record.go` (`encoding/json`, `sql.NullString` → NULL=nil, JSON malformé=nil défensif) ; repos mappent les 4 nouvelles colonnes. `selector_test.go` ajusté (helper `rule(strategy)`/`ruleWithConfig`). **QA** : `go build ./src/...` OK, `go vet` OK, `go test ./src/routing/...` 32 PASS (dont Fastest_ByLatency, Fastest_TieBreakByScore, Custom_WithPreferredOrder, Custom_FallbackToHighestDelivery), go.mod inchangé. **architect-reviewer CONFORME (point critique validé)** : `encoding/json` UNIQUEMENT en C3 (grep domain/ vide), `RoutingConfig` map = type pur acceptable, selector.go logique pure déterministe, 0 import adapters→infra, isolation schéma `routing`. **Stdlib-only.** Note : corrige aussi une erreur de build préexistante de routing (signalée par le go-engineer provider). |
| 2026-06-19 | **D4 permissions text().array()** (auth-api schema.ts) — MINEUR | **PASS / CONFORME** | ts-engineer (même passe que D1). `infrastructure/db/schema.ts` : `permissions: text().array()` (stub local `text()` enrichi d'une méthode `.array()` → `{_type:"text[]"}`), aligné sur la colonne réelle `permissions TEXT[]` de 0011. Style stub cohérent (drizzle-orm reste commenté). QA PASS, architect-reviewer CONFORME (Drizzle confiné C4). |
| 2026-06-14 | T-006 — dette schéma identity (10 colonnes absentes de 0001) + dette T-007 (`POST /validate-session` absent) | **PASS / CONFORME** | Séquence db-engineer → ts-engineer → qa + architect-reviewer (parallèle). **db-engineer** : `migrations/0011_identity_schema.sql` créée (0010 déjà pris par webhook → 0011 = prochain libre), STRICTEMENT additive (`ADD COLUMN IF NOT EXISTS`), un seul schéma `identity`, 0 modification de 0001. 10 colonnes : `workspaces` slug/owner_id/plan, `users` name/role, `api_keys` name/prefix/permissions(TEXT[])/last_used_at/expires_at. UNIQUE sur `slug` via `CREATE UNIQUE INDEX IF NOT EXISTS uq_workspaces_slug` (idempotent, nommé, préféré à contrainte anonyme). `owner_id` nullable sans FK (assumé). **ts-engineer** : 9 fichiers touchés — `infrastructure/db/schema.ts` (10 colonnes Drizzle alignées 0011) ; `workspace-repository.ts`/`user-repository.ts` (mapping bidirectionnel slug/ownerId/plan, name/role, TODO schéma retirés) ; `api-key-repository.ts` (mapping name/prefix/permissions/lastUsedAt/expiresAt + **`updateLastUsed` n'est plus un no-op** : UPDATE Drizzle commenté + throw explicite, pattern stub offline) ; `validate-api-key.ts` (confirmé : `updateLastUsed` appelé sur le chemin de succès, l.42) ; `auth.handler.ts` (route `POST /validate-session` : Bearer header prioritaire + fallback body `{token}` → `authProvider.validateSession` → 200 `{workspaceId,userId,authMethod:"session"}` ou 401, `authProvider` injecté 6e param du constructeur) ; `index.ts` (câblage authProvider→AuthHandler) ; commentaires TODO(schema) obsolètes nettoyés dans domain/. **QA** : `tsc --noEmit` exit 0 sur auth-api ET graphql-api (tsc système 5.9.2, 0 régression) ; 10/10 colonnes alignées schema.ts↔0011 ; grep drizzle-orm/better-auth = 0 import actif (stubs/commentés) ; updateLastUsed plus no-op + appelé en C2 ; `/validate-session` route+Bearer+401+structure conformes ; mocks `InMemoryApiKeyRepository` conformes au port ; jest SKIP (node_modules absent offline) → revue statique PASS. **architect-reviewer** : **CONFORME** — persistence en C3, Drizzle confiné C3/C4, `updateLastUsed` orchestré par use case C2 via port (inversion OK), `/validate-session` en handler C3 délègue au port `AuthProvider` C2 (types purs, pas de fuite better-auth), domaine pur (nouveaux champs = primitifs), 0 import framework actif hors C3/C4, composition root C4→C3→C2, isolation schéma `identity` stricte. **Écarts documentés non bloquants (offline)** : E1 `permissions: text()` au lieu de `text().array()` (limitation stub) ; E2 stub `validateSession` throw → 500 au lieu de 401 en offline (disparaît avec better-auth réel) ; E3 `workspaceId: ""` dans `/validate-session` (TODO résolution userId→workspaceId via UserRepository — dette fonctionnelle résiduelle, voir Blocages). Reste CI : `atlas migrate hash`/`lint` (offline), `npm install` + `make build pkg=auth-api` (esbuild). |
| 2026-06-14 | T-005 — dette schéma webhook (colonnes `active`/`payload`/`next_retry_at` absentes de 0006) | **PASS** | Séquence db-engineer → go-engineer → qa + architect-reviewer (parallèle) → PM (nettoyage commentaires). **db-engineer** : `migrations/0010_webhook_schema.sql` créée, additive (`ADD COLUMN IF NOT EXISTS`), un seul schéma `webhook`, ne modifie aucune colonne de 0006. `atlas migrate hash` non exécuté (Atlas indisponible offline) → à faire en CI ; pas d'`atlas.sum` (génération initiale attendue). **go-engineer** : `record.go` (champs `Active bool`, `Payload []byte`, `NextRetryAt *time.Time`, mappings 2 sens, TODO obsolètes retirés) ; `endpoint_repository.go` (mapping `active` + `FindByWorkspaceAndEvent` filtre `WHERE active = true`) ; `delivery_repository.go` (persiste `payload`/`next_retry_at` en insert/update + nouvelle méthode `FindPendingRetries(ctx, at)` filtrant `next_retry_at IS NOT NULL AND next_retry_at <= $1 AND status='failed'`, garde `db==nil` → `nil,nil`) ; `ports/output/ports.go` (port `DeliveryRepository.FindPendingRetries` ajouté) ; `main.go` (scheduler goroutine ticker 60s → `FindPendingRetries(ctx, time.Now())` → `retryUC.Execute(ctx, d.ID)`, arrêt sur `ctx.Done()`, erreurs loggées sans panic, no-panic en mode dev `db==nil`). Signature de retry réellement câblée : `func (uc *RetryDelivery) Execute(ctx context.Context, deliveryID int64) error`. Un seul test existant ajusté (`deliver_event_test.go` : ajout `FindPendingRetries` au mock pour conformité d'interface — légitime). **QA** : `go vet ./src/webhook/...` OK, `go build ./src/...` OK (0 régression, 15 packages), `go test ./src/webhook/... -count=1` 48 tests PASS (QA a ajouté `persistence/record_test.go`, 6 tests couvrant les mappings). 4 critères fonctionnels confirmés. **architect-reviewer** : **CONFORME** — port en C2 / impl en C3 (inversion OK), 0 import adapters→infrastructure, domaine pur (champs types Go standard), scheduler `main.go` = câblage pur (backoff/éligibilité restent en use case + repo), isolation schéma `webhook`, assertions `var _ output.*Repository` présentes. **PM (post-revue)** : nettoyage des commentaires obsolètes (`domain/delivery.go`, `usecases/retry_delivery.go`) qui référençaient encore 0006/la dette — purement cosmétique, re-build + `go test ./src/webhook/...` re-PASS. |

### Tâches
| Date | Tâche | Verdict | Détails |
|------|-------|---------|---------|
| 2026-07-13 | T-016 (1re passe) | PASS | Séquence : go-engineer → PM (pré-vérif mécanique, indispo modèle) → qa + architect-reviewer (parallèle) → PM (nitpicks). **go-engineer** : port C2 `ContactScoreClient`, client REST C3 `contact_intel_client.go` (HTTP timeout 2s, ne l'importe pas cross-service), fonction pure `domain.CombineScore((ps+cs+1)/2, borné [0,100])`, enrichissement `highest_delivery` sur copie de slice + fallback gracieux dans `GetRoutingDecision`, signature `Execute` +`recipient`, DTO/handler rétrocompat, config `ContactIntelAPIURL`, câblage main.go (nil si URL vide). **QA PASS 9/9** : build/vet exit 0 ; `go test ./src/routing/...` (domaine 20 + usecases 11 + http 19, QA a ajouté 2 tests handler pour la propagation `recipient`) ; 18 pkgs monorepo ok/0 régression ; go.mod inchangé ; **isolation cross-service** grep `^\s+"fleece/src/contact-intelligence"` = 0 (2 mentions = commentaires) ; **fallback** testé client nil (0 appel) / erreur (dégradation sans erreur) / recipient vide (0 appel) ; contact inconnu found=false→50 ; D11 (0 SQL cross-schéma). **architect-reviewer CONFORME** (6/6 points : port pur, client C3 isolé, domaine pur, use case dépend du port, composition root C4→C3→C2, frameworks confinés). **Nitpicks non bloquants** (ligne morte `main.go:83` `_ = &http.Client{...}` réservée ; léger désalignement gofmt `handler.go`) → **soldés par le PM** : ligne retirée + `gofmt -w src/routing/`, re-build/vet/test verts (18 pkgs ok, 0 FAIL). **Verdict global PASS, 0 écart résiduel.** Vigilances : T-015 (fallback consomme `FallbackChain` sans recalcul) ; T-018 (campaign passe `recipient` par destinataire pour bénéficier de l'enrichissement, sinon routage global dégradé) ; T-020 (les événements ne transportent pas le contactScore — enrichir si besoin de tracer l'impact, à arbitrer). |
| 2026-07-12 | T-014 (1re passe) | PASS | Séquence : go-engineer → qa + architect-reviewer (parallèle) → PM (correction observation) → re-vérif. **go-engineer** : adapter Telegram `src/provider/internal/adapters/providers/telegram.go` (+ `telegram_test.go` 20 tests) implémentant `output.Provider` (Send/EstimateCost/GetDeliveryStatus, stub `net/http` + `TODO(production)`) ; `MapTelegramResponseStatus` (pure) ok→Sent/400,403→Rejected/≥500,timeout,inconnu→Failed ; EstimateCost=0 (gratuit) ; capabilities constantes Go ; token via config/env (`TELEGRAM_BOT_TOKEN`, jamais loggé) ; `config.go` +2 champs ; `main.go` registry `"telegram-bot"`. **QA PASS 9/9** : `go build`/`vet ./src/...` exit 0, `go test ./src/provider/...` **64 PASS**, 18 pkgs monorepo ok/0 régression, go.mod/go.sum inchangés, token non loggé, whatsapp/sms/domaine intacts. **architect-reviewer CONFORME** : adapter C3 (domaine+stdlib seuls, 0 import infra, mapping pur), config C4, câblage main.go seul (C4→C3), 0 cross-service, capabilities confinées C3. **Observation commune QA+archi** (assertion via interface anonyme inline au lieu de `var _ output.Provider`) → **soldée par le PM/go-engineer** : remplacée par `var _ output.Provider = (*TelegramBot)(nil)` (+ import C3→C2 autorisé, 0 cycle), re-build/vet/test verts. **Verdict global PASS, 0 écart résiduel.** **Débloque T-015.** Vigilances : T-015 (channel `'telegram'`, fallback) ; T-038 (seed cost=0, score défaut ~75-80 à trancher) ; **dette D27** (token en config/env pas encore via `provider_credentials` AES-GCM — aucun adapter existant ne lit la base, pattern cohérent, TODO tracé) ; DLR Telegram = webhook (adapter distinct futur) ; T-024 Secret K8s. |
| 2026-07-12 | T-019 (1re passe) | PASS | Séquence : db-engineer → qa → PM (correction observation). **Pas d'architect-reviewer** (migration SQL pure, 0 frontière Clean Architecture). **db-engineer** : `migrations/0017_analytics_kpi.sql` (0017 prochain libre), STRICTEMENT additive, schéma `analytics` seul. `message_daily` +`delivery_latency_ms_sum bigint` (latence en SOMME additive) + 3 index perf. **MV `analytics.kpi_daily`** (`WITH NO DATA`) : volumes + 4 KPIs produit (délivrabilité, échec, coût moyen/msg, latence moyenne) protégés division/0 (`CASE WHEN`), requête `FROM analytics.message_daily` uniquement (D11). Index UNIQUE `uq_kpi_daily_dimensions` pour `REFRESH CONCURRENTLY`. Stratégie refresh documentée (1er non-concurrent → périodique CONCURRENTLY). **QA PASS 8/8** : (1) 0017 numéro libre ; (2) 100% additif — grep destructif = 0, 9× `IF NOT EXISTS` ; (3) `git diff` 0009 vide ; (4) **isolation/D11 CRITIQUE** — 0 schéma étranger dans le DDL, MV lit `analytics.message_daily` seul, `REFERENCES` en commentaire only ; (5) 4 KPIs + protection div/0 ; (6) index UNIQUE MV présent (CONCURRENTLY OK) ; (7) `atlas migrate hash`+`validate` EXIT 0, Atlas parse `CREATE MATERIALIZED VIEW` ; (8) style aligné. **Observation QA non bloquante** (commentaires en-tête `NULLIF` vs DDL `CASE WHEN`, sémantiquement équivalents) → **soldée par le PM** : commentaire aligné sur `CASE WHEN`, `atlas migrate hash`/`validate` re-EXIT 0, `atlas.sum` 0017 régénéré (`h1:XDsGqEyE…`). **Verdict global PASS, 0 écart résiduel.** **Débloque T-020 → Vague 1 V1 des schémas complète (T-011/T-013/T-017/T-019 + service T-012).** Vigilances T-020 : consumer UPSERT `message_daily` (`ON CONFLICT DO UPDATE`, clarifier quel événement incrémente `sent`) ; job refresh 1er non-concurrent puis CONCURRENTLY ; use cases lisent `kpi_daily` ; `search_path=analytics`. |
| 2026-07-12 | T-017 (1re passe) | PASS | Séquence : db-engineer → qa → PM. **Pas d'architect-reviewer** (migration SQL pure, 0 frontière Clean Architecture — cohérent avec T-011/T-013). **db-engineer** : `migrations/0016_campaign_execution.sql` (0016 prochain libre), STRICTEMENT additive. Enrichit les 3 tables `campaign` : `campaigns` (+message_body/channel_strategy/estimated_cost bigint/currency/rate_limit), `campaign_recipients` (+status/message_id uuid libre + UNIQUE INDEX dédup `(campaign_id, recipient)`), `campaign_runs` (+total/sent/delivered/failed counters) + 3 index de lecture (workspace, status+scheduled_at pour le scheduler, campaign+status pour reprise). **Décisions** : `estimated_cost` centimes/bigint (aligné wallet) ; `rate_limit` 0=illimité ; `message_id` colonne libre non-FK (corrélation DLR sans cross-schéma, D11) ; dédup enforced en base (index UNIQUE nommé, pas de contrainte anonyme). **QA PASS 7/7** : (1) fichier 0016 numéro libre ; (2) 100% additif — grep `DROP\|ALTER..DROP\|ALTER..TYPE\|RENAME\|DELETE\|TRUNCATE\|UPDATE` = 0 match, 12 `ADD COLUMN IF NOT EXISTS`, 4 index `IF NOT EXISTS` nommés ; (3) `git diff` 0007 vide (intact) ; (4) isolation `campaign` — grep autres schémas = commentaires only, `REFERENCES` seulement en commentaire (0 FK cross-schéma, `message_id` libre) ; (4bis) tous les champs attendus + types cohérents ; (5) dédup idempotente `CREATE UNIQUE INDEX IF NOT EXISTS` ; (6) `atlas migrate hash`+`validate` EXIT 0, `atlas.sum` 0016 `h1:LFDi0Erh…`, `lint` SKIP Pro ; (7) style en-tête aligné 0013/0014/0015 + 12 décisions documentées. **Verdict global PASS, 0 écart.** **Débloque T-018.** Vigilances T-018 : machine à états campagne + dédup `ON CONFLICT` + estimation coût via client REST routing (pas SQL cross-schéma) + scheduler UPDATE atomique anti-double-run + rate_limit token bucket + consumer DLR par `message_id` (gérer NULL) + reprise `status='pending'`. |
| 2026-07-12 | T-013 (1re passe) | PASS | Séquence : db-engineer → qa → PM. **Pas d'architect-reviewer** (migration SQL pure, aucune couche de code / frontière Clean Architecture — cohérent avec T-011). **db-engineer** : `migrations/0015_provider_telegram.sql` (0015 = prochain libre), STRICTEMENT additive (`CREATE SCHEMA IF NOT EXISTS` + `INSERT ... ON CONFLICT (id) DO NOTHING`). Seed provider `id='telegram-bot'`/`channel='telegram'`/`enabled=true`. **Décisions tranchées** : credentials = option (a) réutilise `provider_credentials.secret_enc` (bot token global par provider, symétrie whatsapp-meta/sms-twilio ; insertion hors migration, chiffré AES-GCM, 0 secret en clair) ; capabilities = pas de colonne (constantes Go côté adapter C3 T-014). **QA PASS 8/8** : (1) fichier 0015 numéro libre ; (2) 100% additif — grep `DROP\|ALTER..DROP\|ALTER..TYPE\|RENAME\|DELETE\|TRUNCATE\|UPDATE` = 0 match, seules 2 instructions actives gardées ; (3) `git diff` 0005/0012 vide (intacts) ; (4) isolation `provider` — grep autres schémas = 0, 0 FK cross-schéma ; (5) sécurité — `token` seulement en commentaires, 0 secret en valeur SQL ; (6) seed cohérent convention `{channel}-{vendor}` (vérifié vs `src/provider/main.go` registry whatsapp-meta/sms-twilio) ; (7) `atlas migrate hash`+`validate` EXIT 0, `atlas.sum` 0015 `h1:p99Dg+…`, `lint` SKIP Pro documenté ; (8) style en-tête aligné 0012/0013 + décisions documentées. **Verdict global PASS, 0 écart.** **Débloque T-014.** Vigilances T-014 : registry `"telegram-bot"` + token via `provider_credentials WHERE provider_id='telegram-bot'` (AES-GCM) + capabilities constantes Go. Vigilance T-024 : Secret K8s clé AES-GCM + insertion ligne credentials chiffrée hors migration. |
| 2026-07-12 | T-012 (1re passe) | PASS | Séquence : go-engineer → qa + architect-reviewer (parallèle) → PM. **PASS du 1er coup** (leçons Broker C3 des services MVP appliquées d'emblée). **go-engineer** : service `src/contact-intelligence` complet (27 fichiers), stdlib-only, go.mod/go.sum inchangés. Domaine pur (scoring **Laplace α=1** `floor((succ+1)/(succ+fail+2)*100)`, prior 50, bornes [0,100]) ; use cases `GetContactScore`/`EnrichRecipient`/`RecordDeliveryOutcome` + ports output ; persistence C3 avec `OutcomeStore.RecordAtomically` (upsert `contact_channel_scores` `ON CONFLICT (phone,channel)` + MAJ compteurs `contacts` + append `contact_channel_history` dans un seul `sql.Tx`), Broker C3 + consumer DLR, HTTP `GET /contacts/{phone}/score` + `POST /outcomes` ; infra config(8086)/postgres(`search_path=contact_intel`)/rabbitmq(NoopBroker)/httpserver + composition root `main.go`. **QA PASS 8/8** : `go build ./src/...` exit 0, `go vet ./src/contact-intelligence/...`+`./src/...` exit 0, `go test ./src/contact-intelligence/... -count=1` **29 PASS** (8 domaine + 11 use cases + 10 handler ajoutés par QA), `go test ./src/...` 17 pkgs ok/0 régression, go.mod/go.sum inchangés (grep imports tiers actifs = 0) ; transaction atomique confirmée par lecture (`outcome_store.go` BEGIN→3 ops→COMMIT/rollback différé) ; contrat HTTP validé ; colonnes 0014 exactes ; isolation `contact_intel`. **architect-reviewer CONFORME** (par couche) : domaine C1 pur (imports `errors`/`time` seuls), application C2 ne dépend que des ports+domaine (`sql.Tx` non fuité), adapters C3 assertions `var _ output.*` + Broker C3 + handler délègue, infra C4 NoopBroker assertion + composition root `main.go` seul, isolation stricte, 0 import cross-service. **Observations non bloquantes** : (a) `success_count` par canal reconstruit par inversion (±1 pt, candidat migration 0015) ; (b) parseur `text[]` manuel ; (c) `EnrichRecipient` non exposé ; (d) **depguard/golangci absent du dépôt = dette pré-existante** (contredit CLAUDE.md/ARCHITECTURE.md §7, conformité manuelle seule → devops). **Débloque T-016** (contrat `GET score` documenté). |
| 2026-07-05 | T-011 (1re passe) | PASS | Séquence : db-engineer → qa → PM. **Pas d'architect-reviewer** (migration SQL pure, aucune couche de code / frontière Clean Architecture touchée — cohérent avec T-009/T-010). **db-engineer** : `migrations/0014_contact_intel_scoring.sql` créée (0013=dernier → 0014 prochain libre), STRICTEMENT additive. Enrichit `contact_intel.contacts` (`ADD COLUMN IF NOT EXISTS`) : `country VARCHAR(10) DEFAULT ''`, `last_channel text DEFAULT ''`, `last_success_at TIMESTAMPTZ` (nullable), `success_count`/`failure_count INTEGER DEFAULT 0` (compteurs agrégés → score global sans ré-agréger l'historique). Nouvelle table `contact_intel.contact_channel_scores` (PK `(phone, channel)`, `score INTEGER 0-100`, `sample_size`, `last_success_at`, `updated_at DEFAULT now()`) — **choix table dédiée vs JSONB** motivé (indexabilité `(channel, score DESC)`, granularité de lock sur upsert `(phone,channel)`, lisibilité SQL). 5 index : `idx_contacts_country`, `idx_contacts_country_score (country, delivery_score DESC)`, `idx_channel_scores_phone`, `idx_channel_scores_channel_score (channel, score DESC)`, `idx_channel_history_phone_channel (phone, channel)`. `atlas migrate hash` OK, `atlas.sum` régénéré (entrée 0014 `h1:bUP8Ex6yUbryWGEaVpH/966x1dl/vyq8EQQPJuMn0Xk=`). **QA PASS 7/7 critères** : (1) fichier 0014 numéro libre ; (2) 100% additif — `grep DROP\|ALTER..DROP\|ALTER..TYPE\|RENAME\|DELETE\|TRUNCATE` = 0 match, 14× `IF NOT EXISTS`, 0 instruction additive sans garde ; (3) `git diff` 0008 vide (intact) ; (4) isolation — grep autres schémas = 0, grep `FOREIGN KEY\|REFERENCES` = 0 ; (5) champs T-011 tous présents ; (6) style aligné 0013 (+ séparateurs de sections) ; (7) `atlas migrate hash`+`validate` EXIT 0, `atlas migrate lint` **SKIP documenté** (réservé Atlas Pro depuis v0.38, à faire en CI via `atlas login` — non bloquant, précédent T-009/T-010). **Verdict global PASS, 0 écart.** Dette : `atlas migrate lint` communautaire indisponible offline. Vigilance transmise à T-012 (upsert `ON CONFLICT` + MAJ compteurs même transaction) et T-016 (accès `contact_intel` via REST interne, pas cross-schéma D11). |
| 2026-06-23 | T-010 (1re passe) | PASS | Séquence : qa-engineer → PM (verdict global). **Pas d'architect-reviewer** : T-010 ne produit pas de code applicatif (suite d'acceptance par revue statique + exécution des tests existants), aucune frontière Clean Architecture touchée. **QA** a vérifié les 5 sections offline-safe (sans cluster/DB/Atlas/Docker/npm) : **A** SDL `src/graphql-api/adapters/graphql/schema.graphql` — 5/5 Query + 4/4 Mutation + 7/7 types, `CreateApiKeyResult.rawKey: String!` confirmé ; **B** `src/auth-api/adapters/http/auth.handler.ts` 6/6 routes (rotation au path `/workspaces/:id/api-keys/:keyId/rotate`) ; **C** 6/6 use cases conformes par lecture de code (refs lignes fournies) ; **D** `go build`/`go vet ./src/...` exit 0, `go test ./src/... -count=1` **161 PASS / 0 FAIL** (15 packages, go.mod inchangé stdlib-only), tests TS jest SKIP documenté (offline) ; **E** frontend `src/platform-app` — 9/9 ops `queries.ts` alignées SDL + 0 opération fantôme, rawKey 1×, solde via `walletBalance`, secret masqué. **Verdict global PASS — 0 écart bloquant.** Récapitulatif : tous les critères MVP des stories API-01/02/03/04 + DASH-01/02/03/04 vérifiés ✓. **7 dettes non bloquantes** confirmées (D-E01..07, voir Blocages T-010) — toutes pré-existantes et déjà documentées (T-007 BFF Apollo/Yoga + Query.transactions + secret/rotate ; T-008 workspaceId session + outillage ; T-009 atlas hash). **Clôture du MVP P0 : T-001..T-010 = Done/PASS.** |
| 2026-06-22 | T-009 (1re passe) | PASS | Séquence : devops-engineer → qa → PM. **Pas d'architect-reviewer** : T-009 ne touche ni code applicatif ni frontières Clean Architecture (manifests K8s + CI uniquement). **devops** : 16 fichiers — `deploy/k8s/{namespace,config/{configmap,secret.example},infra/{postgres,rabbitmq},jobs/atlas-migrate,services/{8 svc},README}.yaml` + `.github/workflows/ci.yml`. Choix : ConfigMap `atlas-migrations` créé via `kubectl create configmap --from-file=migrations/` (le bastion ne COPY pas le repo) ; graphql-api+platform-app LoadBalancer (NodePort commenté) ; Go services forcent `PORT` via env[].value (défaut code 8080) ; Secret expose POSTGRES_DSN (Go libpq) + DATABASE_URL (TS/Atlas URL) ; HPA autoscaling/v2 CPU 70% par service ; emptyDir pour postgres/rabbitmq (dev). **QA PASS A-G** : A 16/16 fichiers ; B 15/15 YAML `python3 yaml.safe_load_all` OK (pyyaml installé) + 0 tabulation ; C triple cohérence containerPort↔env PORT↔Service.port/targetPort + noms DNS = URLs ConfigMap, 8/8 ; D env RÉELLES respectées (Go POSTGRES_DSN/SEARCH_PATH par schéma/DRIVER/RABBITMQ_URL hors routing, routing DEFAULT_CURRENCY, auth-api DATABASE_URL, graphql-api 4 URLs, platform-app NEXT_PUBLIC_GRAPHQL_URL, Secret double DSN+secrets provider) ; E job Atlas image bastion + `atlas migrate apply --dir file:///migrations --url $(DATABASE_URL)` + ConfigMap /migrations + restartPolicy Never + initContainer wait-for-postgres ; F images `fleece/<svc>:latest` 8/8 + `command:["node",...]` auth-api/graphql-api (node.dockerfile sans ENTRYPOINT) + platform-app CMD Dockerfile ; G CI build/vet/test Go (1.26.4 aligné go.mod), `make build pkg=auth-api|graphql-api`, `atlas migrate hash`(+drift+validate), `make build pkg=platform-app`, runs-on ubuntu-latest. **Écarts** : (1) commentaire trompeur `routing.yaml` l.47 (RABBITMQ_URL injecté via envFrom mais non consommé) → **CORRIGÉ par le PM** ; (2) `make build pkg=rest-api` absent du CI (rest-api scaffolé mais hors-scope T-009) → dette. **Offline** : `kubectl apply --dry-run`/`docker build`/`atlas migrate hash` non exécutés localement (cluster/réseau absents) → à valider en CI online. |
| 2026-06-22 | T-008 (1re passe) | PASS | Séquence : devops-engineer (prérequis) → frontend-engineer → qa + architect-reviewer (parallèle). **devops** : `mk/react.mk` (cibles deps/build/test/fmt calquées sur node.mk ; `build` = `tsc --noEmit` + `bash -c 'cd ./src/${pkg} && npm exec -- next build'` car Next lit next.config depuis cwd et shell=fish ; `has_image`/`target=dist`/`dockerfile=docker/react.dockerfile`), `docker/react.dockerfile` (multi-stage Next standalone : build node:21-slim `make build pkg=$PKG`, dist copie `.next/standalone`+`.next/static`, EXPOSE 3000, HOSTNAME=0.0.0.0, `CMD node server.js`), `./src/platform-app` ajouté aux workspaces npm. `make --dry-run build/image/test pkg=platform-app` OK ; Dockerfile parsé OK par Docker. **frontend** : 34 fichiers Next.js App Router écrits à la main (offline, sans create-next-app). Config `next.config.js` `output:'standalone'` confirmé, tsconfig `@/*`, package.json `@fleece/platform-app`. Couche données `lib/graphql/` (client `fetch` POST→`NEXT_PUBLIC_GRAPHQL_URL` fallback localhost:4000/graphql, queries.ts documents bruts, types.ts alignés SDL) — AUCUNE dépendance Apollo/urql. UI inline shadcn-like `components/ui/{Button,Card,Badge,Input,Table,Dialog,Toast}` + `components/states/{Loading,Empty,Error}` + Sidebar/Header. globals.css = tokens DESIGN.md exacts (dark défaut + thème clair). Écrans : DASH-01 signup (card 392px radial-gradient) + onboarding stepper 4 étapes (Workspace→API Key→Wallet→Send, rawKey 1×) ; DASH-02 api-keys liste/create(modal rawKey 1×)/revoke(confirm)/rotate(revoke+create) ; DASH-03 wallet solde(centimes/100+Intl currency)/recharger placeholder/historique empty-state ; DASH-04 webhooks liste/add(url https+events)/delete(confirm)/secret masqué/livraisons empty-state. **QA PASS** : A 34/34 fichiers + `output:'standalone'` + `type=react` ; B grep drizzle/better-auth/pg/apollo/urql = commentaires seuls (0 import), types front↔SDL alignés, 0 appel GraphQL fantôme (rotateApiKey/Query.transactions/secret/deliveries/topUp traités en UI placeholder ou empty-state, jamais en query/mutate) ; C DASH-01..04 happy path + dark theme tokens vérifiés 1:1 vs DESIGN.md ; D tsc SKIP (node_modules absent + tsc système absent, offline). **architect-reviewer CONFORME** : 0 import backend/cross-package/internal, données 100% via `lib/graphql/client.ts`→BFF, `type=react` intact, pas de services/libs/apps. Non bloquant : `app/auth/{login,signup}` appellent `/api/auth/*` (chemins same-origin Better Auth, pas une URL de service — légitime, hors GraphQL). **Offline** : aucun `npm install`/`next build`/`tsc` exécuté → dette outillage. |
| 2026-06-13 | Baseline scaffold | PASS (partiel) | `go vet`/`go build ./src/...` OK ; `make build pkg=messaging` OK. Couches TS/DB/Docker en place. Adapters concrets à venir. |
| 2026-06-13 | T-001 (1re passe) | FAIL | Build/vet/tests OK (QA). Architect-reviewer: 2 violations C3→C4 — `adapters/publisher` et `adapters/consumer` importaient `infrastructure/rabbitmq` (interface `Broker` mal placée en C4). Renvoyé au go-engineer. |
| 2026-06-13 | T-001 (après correction) | PASS | Interface `Broker` rapatriée en C3 (`internal/adapters/messaging/broker.go`), `NoopBroker` reste en C4 avec assertion infra→C3 (sens autorisé). `go vet ./src/messaging/...` OK, `go build ./src/...` OK, `go test ./src/messaging/...` 10/10 PASS. Aucun adapter n'importe l'infra (grep CLEAN). go.mod inchangé. Frontières CONFORMES. |
| 2026-06-13 | T-003 (1re passe) | PASS | go-engineer → qa → architect-reviewer, PASS du 1er coup (leçons T-001/T-002 sur le placement des interfaces appliquées d'emblée). QA : `go build ./src/...` OK, `go vet ./src/routing/...` OK, `go test ./src/routing/... -v` 35/35 PASS (le QA a ajouté `internal/adapters/http/handler_test.go`, 17 cas couvrant les 2 endpoints + mapping erreurs 400/422/500/204). go.mod inchangé, go.sum absent (stdlib-only), aucun import tiers actif. Isolation schéma `routing` stricte (3 tables, colonnes exactement conformes à 0004). Architect-reviewer : CONFORME — domaine pur (selector.go logique pure), application ne dépend que des ports, 0 violation adapters→infrastructure (grep vide), pas d'interface Broker (Routing ne publie pas), inversion via ports, aucun import cross-service. Écarts schéma/spec assumés et documentés (devise par config, Fastest/Custom en fallback faute de colonnes latence/config). |
| 2026-06-13 | T-004 (1re passe) | PASS | go-engineer → qa → architect-reviewer, PASS du 1er coup (leçons T-001/T-002 sur le placement du Broker appliquées d'emblée). QA : `go build ./src/...` OK, `go vet ./src/provider/...` OK, `go test ./src/provider/... -count=1` 35/35 PASS (QA a ajouté handler_test.go 14 cas mapping HTTP 400/404/502/503/500/204, handle_dlr_test.go 6 cas, sms_twilio_test.go 8 cas). go.mod inchangé, go.sum absent (stdlib-only), aucun import tiers actif (`lib/pq` en commentaire uniquement). Critères : port `Provider` (Send/EstimateCost/GetDeliveryStatus) OK ; Dispatch (ProviderNotFound/sent/failed) OK ; HandleDLR (delivered/failed, statuts invalides rejetés) OK ; stubs providers externalId non vide + Money valide sans réseau réel OK ; persistence colonnes exactes 0005 OK ; config port 8084 OK. Architect-reviewer : CONFORME — domaine pur, application ne dépend que des ports, 0 violation adapters→infrastructure (grep vide), interface `Broker` en C3 (`adapters/messaging/broker.go`) + NoopBroker en C4 avec assertion, port `Provider` en C2 implémenté par adapters C3 (inversion OK), isolation schéma `provider` stricte, aucun import cross-service. **Écart schéma documenté** : 0005 minimal → Name/Country non persistés, Active↔`enabled`, pas de colonne status → HandleDLR publie sans persister le statut (TODO). Recommandations non bloquantes : assertions `var _ output.Provider` locales dans les adapters ; vérifier couverture depguard `src/provider` en CI. |
| 2026-06-14 | T-005 (1re passe) | PASS | go-engineer → qa → architect-reviewer, PASS du 1er coup (leçons Broker C3 appliquées d'emblée). QA : `go build ./src/...` OK, `go vet ./src/webhook/...` OK, `go test ./src/webhook/... -count=1` 41/41 PASS (QA a ajouté register_endpoint_test 7, deliver_event_signature_test 4 — vérifie que DeliverEvent émet un HMAC-SHA256 réellement vérifiable —, array_test 4, handler_test 13), 0 régression sur les autres services. go.mod inchangé, go.sum absent (stdlib-only), grep C3→C4 vide. Critères : Sign/Verify (nominal/mauvais secret/payload modifié/malformé) OK, hmac.Equal (temps constant) ; DeliverEvent (2xx→Delivered, non-2xx→Failed+retry, inactif ignoré, no-op si 0 abonné) OK ; RetryDelivery (backoffFor 1m/5m/30m/2h/8h, 6e→Exhausted+ErrMaxRetriesExceeded) OK ; RegisterEndpoint (url vide/0 event rejetés, secret crypto/rand) OK. Architect-reviewer : CONFORME — domaine pur (signature.go stdlib sans I/O), application ne dépend que des ports, 0 violation C3→C4 (seul un commentaire mentionne infrastructure), interface Broker en C3 (`adapters/webhook/broker.go`) + NoopBroker C4 avec assertion, inversion par ports, isolation schéma `webhook` stricte, aucun import cross-service. **Correctifs post-revue appliqués par go-engineer** : assertions `var _ output.{Endpoint,Delivery}Repository` ajoutées aux repos ; dette payload rendue explicite via `ErrEmptyPayload` + guard dans retry_delivery.go + TODO(schema). Re-test 29/29 PASS après correctifs. **Dette à arbitrer (→ db-engineer)** : aligner 0006 (ajouter payload + next_retry_at + active) avant de câbler un scheduler de retry, sinon RetryDelivery ne peut redispatcher un body fiable. |
| 2026-06-14 | T-007 (1re passe) | PASS | ts-engineer → architect-reviewer + qa, PASS du 1er coup. ts-engineer : 20 fichiers (8 modifiés : tsconfig/index/server/clients.ts/get-workspace-overview/identity.client/resolvers index/schema.graphql ; 12 créés : graphql.d.ts, use-cases list-api-keys/manage-webhook, context.ts, 3 résolveurs, 3 clients wallet/messaging/webhook, config.ts, session-validator.ts). `tsc --noEmit -p tsconfig.json` exit 0 (tsc système 5.9.3, paths @fleece/api-common, skipLibCheck) ; auth-api re-typecheck OK (pas de régression). Architect-reviewer : **CONFORME** — application/ pure (uniquement @fleece/api-common + ports internes, 0 framework, 0 import vers C3/C4), SessionValidator port en api-common + HttpSessionValidator en infrastructure (assertion _conformance), 4 clients REST en adapters/clients implémentant les ports C2 (inversion OK + _conformance), schéma+résolveurs+context en adapters/graphql (résolveurs délèguent sans logique métier, SDL↔resolver map parfaitement alignés 5 Query/4 Mutation), serveur en infrastructure, index.ts seul composition root (DI C4→C3→C2), imports relatifs .js + api-common sans .js, Apollo/Yoga/graphql 100% commentés. QA : Section A 20/20 fichiers ; B cohérence interfaces/imports OK (ctx en 1er param, implements + _conformance, délégation, buildContext, câblage index) ; C schéma valide (12/12 accolades, tous types définis, Query/Mutation complètes, PageInfo cursor) ; D garde-fous offline (grep Apollo/Yoga/graphql commentés, fetch+node:* seuls actifs, application/ sans framework) ; E typecheck exit 0. **Dettes non bloquantes** (voir Blocages) : TransactionPage défini sans Query.transactions (DASH-03), package @anthill/graphql-api à harmoniser @fleece/, /validate-session absent côté auth-api (HttpSessionValidator le cible), Apollo/Yoga à installer + décommenter pour runtime, stubs clients retournent null/vide (TODO production). |
| 2026-06-14 | T-006 (1re passe) | PASS | ts-engineer → architect-reviewer → qa, PASS du 1er coup. ts-engineer : 18 fichiers (4 domaine, 1 ports + 5 use cases, 4 adapters auth/persistence/http, 2 infra db, index.ts) + 3 tests __tests__ ; `tsc --noEmit` OK via tsc système (5.9.3) avec `paths` → `@fleece/api-common` source locale (offline, node_modules absent). Architect-reviewer : CONFORME — domain/ zéro import, application/ uniquement domain + @fleece/api-common (type pur ApiContext) + node:crypto, port AuthProvider à types purs (pas de fuite better-auth), Better Auth confiné adapters/auth + infrastructure/db, Drizzle confiné persistence + db/schema.ts, inversion par ports (assertions `_conformance`), aucun import npm actif non installé (better-auth/drizzle 100% commentés). QA : Section A 18/18 fichiers présents ; Section B 6/6 (ApiKey isActive=active&&!isExpired, 5 errors, create-api-key fk_<prefix>_<random>+SHA-256 ne persiste que le hash, validate-api-key→ApiContext api_key, rotate=revoke+create, handler routes+mapping 400/401/403/404) ; Section C 4/4 garde-fous (grep domain/ propre, application/ sans framework vide, aucun import npm actif, Better Auth dans adapters/infra/index uniquement) ; Section D tsc/jest SKIP documenté (offline, aucun binaire local). **Dette schéma 0001 documentée** (→ db-engineer) : 10 colonnes d'entités domaine non persistables (slug/ownerId/plan, name/role, name/prefix/permissions/last_used_at/expires_at) → valeurs par défaut à la lecture + updateLastUsed no-op silencieux ; migration à aligner avant production. Observations non bloquantes : `paths` tsconfig (divergence potentielle esbuild/tsc), nom package `@anthill/auth-api` à harmoniser. |
| 2026-06-13 | T-002 (1re passe) | PASS | go-engineer → qa → architect-reviewer, PASS du 1er coup (leçon T-001 sur le Broker appliquée d'emblée). QA : `go build ./src/...` OK, `go vet ./src/wallet/...` OK, `go test ./src/wallet/... -v` 19/19 PASS, grep C3→C4 vide, go.mod/go.sum inchangés. Critères fonctionnels Debit/Credit/Refund/GetBalance + mapping HTTP (400/402/404) + Money + persistence (colonnes 0002) tous OK. Architect-reviewer : CONFORME (interface Broker en C3 `adapters/messaging`, NoopBroker en C4 avec assertion C4→C3, inversion via ports, isolation schéma `wallet`, stdlib-only). Écart non bloquant : tests use case credit/refund/get_balance et adapters absents. |

---

## Blocages / Risques
- **Réseau non garanti** : `npm install` (build TS), commandes Atlas/Docker peuvent échouer hors ligne → acceptance partielle à signaler.
- **Dette PRÉ-EXISTANTE — depguard/golangci absent** (relevée par l'architect-reviewer en T-012, non introduite par T-012) : aucun `.golangci.yml`/config depguard dans le dépôt, alors que CLAUDE.md et ARCHITECTURE.md §7 affirment que la règle de dépendance Clean Architecture est « enforced by depguard (Go) in CI ». La conformité est aujourd'hui vérifiée **manuellement** (architect-reviewer) — rien n'empêche une régression automatique. **Action recommandée → devops** : créer `.golangci.yml` (règles depguard interdisant `internal/adapters`+`internal/infrastructure` depuis `internal/domain`+`internal/application`) + l'exécuter dans le job Go du CI. Idem `dependency-cruiser` côté TS à vérifier. Candidat à une tâche dédiée (hors scope V1 feature).
- **Dette D27 — credentials provider en config/env, pas via `provider_credentials`** (constatée en T-014) : la décision D27 prévoit le stockage des bot tokens chiffrés AES-GCM dans `provider.provider_credentials`, mais AUCUN adapter du service `provider` (WhatsApp/SMS/Telegram) ne lit la base — tous reçoivent leur token via la config/env (pattern réel du code). L'adapter Telegram suit ce pattern (env `TELEGRAM_BOT_TOKEN`) avec `TODO(production)` pointant vers `provider_credentials`. **Action** : quand le mécanisme de credentials chiffrés en base sera implémenté (dépend de T-024 DevOps : Secret K8s clé AES-GCM), câbler la lecture `provider_credentials` (déchiffrement) pour TOUS les adapters, pas seulement Telegram. Non bloquant (les tokens transitent par Secret K8s → env en attendant). Dette transverse au service provider.
- **T-009 dettes K8s/CI** (non bloquantes) :
  1. **Offline non validé runtime** : `kubectl apply --dry-run=client`, `docker build` des 9 images, `atlas migrate hash` non exécutés localement (pas de cluster/docker/atlas) → à valider au 1er passage CI online + 1er déploiement.
  2. **`make build pkg=rest-api` absent du CI** : `src/rest-api` (gateway REST public, D22) est scaffolé mais son build n'est pas dans `.github/workflows/ci.yml` (job `ts` ne couvre que auth-api + graphql-api). À ajouter quand rest-api sera implémenté.
  3. **Storage éphémère dev** : postgres/rabbitmq en `emptyDir` (données perdues au restart du pod). PVC commenté dans `infra/postgres.yaml`. Production gérée séparément (cf. brief).
  4. **ConfigMap `atlas-migrations` non versionné en YAML** : créé impérativement via `kubectl create configmap atlas-migrations --from-file=migrations/` (+ `atlas-hcl --from-file=atlas.hcl`) avant le Job — documenté dans `deploy/k8s/README.md` et l'en-tête du Job. À automatiser (kustomize configMapGenerator ou Makefile target) plus tard.
  5. **`NEXT_PUBLIC_GRAPHQL_URL`** = `http://graphql-api:4000/graphql` (DNS interne) : valable pour le SSR ; à remplacer par l'URL externe du LoadBalancer graphql-api pour les appels côté navigateur une fois le cluster actif.
- ~~**`mk/react.mk` + `docker/react.dockerfile` manquants**~~ **RÉSOLU le 2026-06-22** (T-008) : `mk/react.mk` (Next standalone) + `docker/react.dockerfile` (multi-stage Next) créés ; `./src/platform-app` ajouté aux workspaces npm.
- **T-008 dettes dashboard** (non bloquantes) :
  1. **Dette BFF — opérations GraphQL manquantes** (à exposer côté `src/graphql-api` pour câbler le front, traitées en UI placeholder/empty-state) : (a) `rotateApiKey(workspaceId,keyId): CreateApiKeyResult!` → actuellement front fait revoke+create non atomique ; (b) `Query.transactions(workspaceId,cursor,limit): TransactionPage!` → historique wallet en empty-state (le type existe au SDL mais pas la Query) ; (c) `WebhookEndpoint.secret`/`secretPreview` → secret affiché `••••` ; (d) livraisons webhook (`WebhookEndpoint.deliveries` ou Query) → empty-state ; (e) mutation top-up wallet (Mobile Money/Stripe) → modal placeholder ; (f) `createWorkspace` (onboarding étape 1) → toast local, pas d'appel.
  2. **Dette outillage offline** : `npm install` + `make build pkg=platform-app` (`tsc --noEmit` + `next build`) + `make image pkg=platform-app` NON exécutés (node_modules absent, tsc système absent). À re-valider en CI online. Composants `components/ui/*` = stubs inline shadcn-like → remplacer par shadcn réel (`npx shadcn-ui add ...`) une fois online. Fonts Geist : fallback CSS system-ui/monospace (pas de `next/font`/`npm install geist` offline).
  3. **`output:'standalone'` en workspace monorepo** : Next place `server.js` sous `.next/standalone/src/platform-app/` → le `docker/react.dockerfile` (chemins `/.next/standalone/`) pourra nécessiter un ajustement du sous-chemin `server.js`/WORKDIR à la 1re image réelle. À valider au 1er `make image` online.
  4. **Session/workspaceId** : pages utilisent un placeholder `WORKSPACE_ID_FROM_SESSION` → à brancher sur la session (cookie Better Auth + `/validate-session`, D1 désormais soldée côté BFF/auth-api) via un contexte serveur.
  5. **Runtime BFF dépendant** : le dashboard consomme `src/graphql-api`, dont Apollo/Yoga restent à installer+décommenter (Blocage T-007.1) et clients REST à décacheter (T-007.2). Le dashboard ne servira de vraies données qu'une fois ce runtime activé.
- **T-010 dettes acceptance MVP** (non bloquantes, toutes pré-existantes — recensées par la suite d'acceptance, renvoient à T-007/T-008/T-009) :
  1. **D-E01** `Query.transactions(...)` absente du SDL alors que `TransactionPage` est défini → historique wallet en empty-state (= dette T-007.4).
  2. **D-E02** `WebhookEndpoint.secret`/`secretPreview` absent du SDL → secret affiché `••••` statique côté front (= dette T-008.1c).
  3. **D-E03** mutation `rotateApiKey` absente du SDL → rotation = revoke+create non atomique côté front (= dette T-008.1a).
  4. **D-E04** tests jest TS non exécutables offline (node_modules absent) → couverture auth-api validée par revue statique uniquement.
  5. **D-E05** Apollo/Yoga non installés → BFF `graphql-api` répond 501, clients REST encore stubés (= Blocage T-007.1/.2).
  6. **D-E06** placeholder `WORKSPACE_ID_FROM_SESSION` dans les pages dashboard → workspaceId à brancher sur la session (backend `/validate-session` désormais prêt, D1 soldée) (= dette T-008.4).
  7. **D-E07** `atlas migrate hash` non exécuté offline (migrations 0010..0013) → pas d'`atlas.sum` à jour, à valider en CI.
- **Base unifiée vs 99.9 %** : point de couplage assumé (cf. MEMORY D11) ; scindable par schéma plus tard.
- ~~**T-006 dette schéma identity**~~ **RÉSOLUE le 2026-06-14** : migration additive `migrations/0011_identity_schema.sql` (0010 déjà pris par webhook) ajoute les 10 colonnes manquantes (`workspaces.slug/owner_id/plan`, `users.name/role`, `api_keys.name/prefix/permissions/last_used_at/expires_at`), Drizzle `schema.ts` + 3 repos alignés (mapping bidirectionnel, défauts à la lecture retirés), `updateLastUsed` n'est plus un no-op (UPDATE réel câblé, throw explicite en stub offline) et est appelé par `validate-api-key.ts`. Verdict acceptance **PASS** + frontières **CONFORMES**. ~~**Dette fonctionnelle résiduelle** : `/validate-session` retourne `workspaceId: ""`~~ **RÉSOLUE le 2026-06-19** (voir D1 ci-dessous). Reste CI : `atlas migrate hash`/`lint` (offline), `npm install` + `make build pkg=auth-api`.
- ~~**D1 ValidateSession workspaceId**~~ **RÉSOLUE le 2026-06-19** : use case C2 `validate-session.ts` orchestre `authProvider.validateSession` → `userRepository.findById` → `ApiContext` complet (`workspaceId` réel via `user.workspaceId`). `POST /validate-session` délègue au use case (401 sur Unauthorized, mappeur global corrigé 403→401). `graphql-api/infrastructure/session-validator.ts` : `fetch` vers auth-api ACTIVÉ + typé. **PASS / CONFORME.** Reste runtime : Apollo/Yoga à installer+décommenter pour servir le BFF (Blocage T-007.1), clients REST encore stubés (T-007.2).
- ~~**D2 Provider DLR status**~~ **RÉSOLUE le 2026-06-19** : migration additive `0012_provider_dlr_status.sql` (`provider.provider_messages` +`status`/`updated_at` ; `external_id` déjà en 0005). Port `ProviderRepository.UpdateStatus(ctx, externalId, status, updatedAt)` + impl `UPDATE WHERE external_id=$1` ; `handle_dlr.go` persiste le statut après publication (delivered/failed/rejected). **PASS / CONFORME.** Reste CI : `atlas migrate hash`.
- ~~**D3 Routing Fastest/Custom**~~ **RÉSOLUE le 2026-06-19** : migration additive `0013_routing_strategies.sql` (`provider_scores` +`avg_latency_ms`/`country` ; `routing_rules` +`channel`/`config JSONB`). `selector.go` implémente réellement Fastest (tri latence ASC + tie-break) et Custom (`Config.preferred_order`, fallback HighestDelivery) ; `SelectProvider` reçoit désormais `RoutingRule`. Décodage JSONB confiné en C3. **PASS / CONFORME.** Reste CI : `atlas migrate hash`.
- ~~**D4 permissions text().array()**~~ **RÉSOLUE le 2026-06-19** : `schema.ts` aligné sur `permissions TEXT[]` (0011) via stub `.array()`. **PASS / CONFORME.**
- **T-006 `paths` tsconfig auth-api** : `@fleece/api-common` résolu via `baseUrl`+`paths` (béquille offline car node_modules non lié). esbuild résout par node_modules → risque de divergence build/type-check si le symlink workspace diffère du `paths`. À retirer ou aligner (alias esbuild) une fois `npm install` exécuté en CI. Non bloquant.
- **T-006 build TS non exécuté** : `make build pkg=auth-api` (esbuild) + `tsc` projet local non exécutés (offline, node_modules absent). `tsc --noEmit` validé via un tsc système hors projet uniquement. À re-valider en CI online.
- **T-007 dettes BFF GraphQL** (non bloquantes, runtime à activer) :
  1. **Apollo/GraphQL Yoga non installés** : `server.ts` est un stub `GraphQLServerLike` (répond 501) ; le câblage schéma→résolveurs→contexte est écrit en commentaires `TODO(production)`. Installer `@apollo/server` (ou `graphql-yoga`) + décommenter ~2 lignes pour activer le runtime. → tâche devops/ts à planifier avant T-008 (le dashboard consomme ce BFF).
  2. **Stubs clients REST** : les 4 clients (`fetch` global commenté) retournent `null`/listes vides → en l'état aucune donnée réelle, le service ne sert pas en runtime tant que les stubs ne sont pas décachetés.
  3. ~~**`/validate-session` absent côté auth-api**~~ **RÉSOLUE le 2026-06-14** (route ajoutée) **+ 2026-06-19** (D1 : `workspaceId` réel via use case ValidateSession + `HttpSessionValidator.validate` fetch ACTIVÉ et typé). Plus de stub `null`, plus de `workspaceId: ""`. Reste uniquement le runtime BFF (Apollo/Yoga, point 1).
  4. **`TransactionPage` défini sans `Query.transactions`** : le type pagination des transactions existe dans le schéma mais n'est pas exposé en Query (DASH-03 historique wallet incomplet côté GraphQL). À compléter (schéma + résolveur wallet) pour l'écran historique transactions.
  5. **Nom package `@anthill/graphql-api`** (comme auth-api) à harmoniser en `@fleece/graphql-api`. Cosmétique, non bloquant.
  6. **Token service-to-service** : les clients propagent `X-Workspace-Id` mais l'auth inter-services (HMAC/JWT service) reste `TODO(production)`.
- ~~**T-005 dette schéma webhook**~~ **RÉSOLUE le 2026-06-14** : migration additive `0010_webhook_schema.sql` (ajout `webhook_endpoints.active` + `webhook_deliveries.payload`/`next_retry_at`), adapter persistence aligné (mapping Active/Payload/NextRetryAt dans les deux sens, plus de `true` forcé), nouveau port `DeliveryRepository.FindPendingRetries(ctx, at)` + implémentation (filtre `next_retry_at <= at AND status='failed'`), et **scheduler de retry câblé** dans `main.go` (ticker 60s → FindPendingRetries → retryUC.Execute). Guard `ErrEmptyPayload` conservé. Verdict acceptance **PASS** + frontières **CONFORMES**. Reste : exécuter `atlas migrate hash` en CI (offline localement, pas d'`atlas.sum` encore).

---

## Changelog
- **2026-06-23** — **Planification du backlog V1 (🟡 P1) et V2 (🔵 P2)** par le PM. MVP P0 clôturé (T-001..T-010 Done/PASS) → découpe des phases suivantes en tâches atomiques traçables, **toutes au statut `Backlog`, acceptance `—`** (aucune implémentation déclenchée — planification pure). **V1 (T-011..T-025, ~3 mois)** : Contact Intelligence (T-011 schéma → T-012 service scoring joignabilité), Telegram (T-013 schéma → T-014 adapter provider → T-015 routing+fallback), enrichissement routage par le score CI (T-016), Campagnes (T-017 schéma → T-018 service planif/exécution), Analytics avancées (T-019 vues matérialisées → T-020 service KPI/ingestion), exposition BFF Analytics+Campaigns (T-021), dashboard DASH-05 Analytics (T-022) + DASH-06 Campaigns (T-023), DevOps manifests/CI des 3 services V1 (T-024), QA jalon V1 (T-025). **V2 (T-026..T-037, ~6 mois)** : SSO Better Auth (T-026 + dashboard T-027), Messenger (T-028 schéma → T-029 adapter), RCS (T-030 adapter), routage Messenger/RCS+fallback (T-031), routage IA scoring ML (T-032) + A/B testing/feedback (T-033), dashboard V2 (T-034), BFF V2 SSO/canaux/A-B (T-035), DevOps infra ML+secrets SSO (T-036), QA jalon V2 (T-037). **Dépendances clés tracées** : chaîne `schéma → service Go/TS → BFF → dashboard → QA` ; Telegram (T-014) avant routage (T-015) ; Contact Intelligence (T-012) débloque l'enrichissement routage `highest_delivery` (T-016) ; le **routage IA V2 (T-032/T-033) dépend des données produites par Contact Intelligence (T-012) et Analytics (T-020) en V1** ; les adapters Messenger/RCS (T-029/T-030) réutilisent le pattern adapter Telegram (T-014). **Convention** : migrations en pattern additif `00NN`, séquence agents `db → go/ts → qa → architect-reviewer → devops`. **À reporter dans MEMORY.md** : backlog V1/V2 planifié (T-011..T-037), V1 = extension du pipeline existant (nouveaux services Go + nouveau canal + 2 écrans dashboard), V2 = nouveaux canaux + couche IA dont les features proviennent des services V1 (prérequis dur Contact Intelligence + Analytics).
- **2026-06-23** — T-010 **Done** (PASS) — **Clôture du MVP P0**. Suite de tests d'acceptance MVP (transverse) livrée par qa-engineer. Séquence : qa-engineer → PM (verdict global). **Pas d'architect-reviewer** : la tâche ne produit pas de code applicatif (acceptance par revue statique + exécution des tests existants), aucune frontière Clean Architecture concernée. Vérification **offline-safe** (sans cluster K8s, DB, Atlas, Docker ni `npm install`) de 5 sections couvrant les stories API-01..04 + DASH-01..04 : **A** SDL `src/graphql-api/adapters/graphql/schema.graphql` — 5/5 Query (workspace/apiKeys/walletBalance/webhookEndpoints/messages) + 4/4 Mutation (createApiKey/revokeApiKey/createWebhookEndpoint/deleteWebhookEndpoint) + 7/7 types (Workspace, ApiKey, CreateApiKeyResult.rawKey, WalletBalance, WebhookEndpoint, Transaction, Message) ; **B** `src/auth-api/adapters/http/auth.handler.ts` — 6/6 routes (POST /workspaces, POST/DELETE /workspaces/:id/api-keys/:keyId, POST .../rotate, POST /validate-key, POST /validate-session) ; **C** 6/6 use cases conformes par lecture (create-api-key rawKey 1×/SHA-256/hash-only, validate-api-key→ApiContext api_key, webhook deliver_event HMAC-SHA256+hmac.Equal, retry_delivery backoff 1m/5m/30m/2h/8h max 5→Exhausted, wallet debit ErrInsufficientFunds solde≥0, routing selector 4 stratégies+ErrInvalidStrategy) ; **D** `go build`/`go vet ./src/...` exit 0, `go test ./src/... -count=1` **161 PASS / 0 FAIL** (15 packages : messaging 10, provider 44, routing 54, wallet 19, webhook 48 ; analytics/campaign/contact-intelligence sans tests = scaffold V1/V2), go.mod inchangé (stdlib-only), tests jest TS SKIP documenté (offline) ; **E** frontend `src/platform-app` — 9/9 opérations `lib/graphql/queries.ts` alignées SDL + **0 appel GraphQL fantôme** (rotate=revoke+create UI, transactions/secret/top-up en empty-state/placeholder), api-keys rawKey affiché une seule fois (modal détruit à la fermeture), wallet solde via `walletBalance` (centimes/100 + devise ISO), webhooks secret `••••` masqué. **Verdict global PASS — 0 écart bloquant.** **7 dettes non bloquantes** (D-E01..07, voir Blocages T-010), toutes pré-existantes et déjà tracées en T-007/T-008/T-009 (Query.transactions, WebhookEndpoint.secret, rotateApiKey, jest offline, Apollo/Yoga runtime, workspaceId session, atlas hash). **Bilan : MVP P0 (T-001..T-010) intégralement Done/PASS.** **À reporter dans MEMORY.md** : (a) MVP clôturé — acceptance globale des stories API-01..04 + DASH-01..04 PASS ; (b) la suite d'acceptance est statique/structurelle offline (SDL↔front, routes↔use cases, exécution `go test`) → modèle réutilisable pour les jalons V1/V2 ; (c) reste à activer en CI online : runtime BFF (Apollo/Yoga + clients REST décachetés), `npm install`+jest+esbuild, `atlas migrate hash`, branchement workspaceId session — prérequis d'un MVP « runtime-complet » au-delà du MVP « contrat-complet » actuel.
- **2026-06-22** — T-009 **Done** (PASS). DevOps : manifests Kubernetes + init job Atlas + CI GitHub Actions livrés par devops-engineer (16 fichiers). Séquence : devops-engineer → qa → PM (pas d'architect-reviewer : aucun code applicatif ni frontière Clean Architecture concernée). **`deploy/k8s/`** : `namespace.yaml` (ns `fleece`) ; `config/configmap.yaml` (`fleece-config` : URLs inter-services AUTH/WALLET/MESSAGING/WEBHOOK_API_URL + ROUTING/WALLET/PROVIDER_URL, POSTGRES_DRIVER, RABBITMQ_URL, DEFAULT_CURRENCY=XAF, NEXT_PUBLIC_GRAPHQL_URL) + `config/secret.example.yaml` (template stringData : POSTGRES_DSN libpq + DATABASE_URL URL + Twilio/WhatsApp) ; `infra/postgres.yaml` (postgres:16, Service ClusterIP `postgres:5432`, emptyDir dev, PVC commenté) + `infra/rabbitmq.yaml` (rabbitmq:3-management, `rabbitmq:5672` + 15672) ; `jobs/atlas-migrate.yaml` (image `fleece/bastion:latest`, `atlas migrate apply --dir file:///migrations --url $(DATABASE_URL)`, ConfigMap `atlas-migrations` monté /migrations, initContainer wait-for-postgres busybox, restartPolicy Never, backoffLimit 3) ; `services/<svc>.yaml` ×8 (Deployment+Service+HPA) — noms Service K8s = DNS exact, ports messaging 8081/wallet 8082/routing 8083/provider 8084/webhook 8085/auth-api 3001/graphql-api 4000/platform-app 3000, ClusterIP sauf graphql-api+platform-app en LoadBalancer (NodePort commenté), tcpSocket probes, initContainer wait-for-postgres sur les services DB ; `README.md` (ordre d'application, création des ConfigMaps migrations/atlas-hcl, prérequis images). **Env RÉELLES respectées** : services Go via `POSTGRES_DSN` (key=value libpq, secretKeyRef) + `POSTGRES_SEARCH_PATH` (schéma propre par service) + `POSTGRES_DRIVER` + `RABBITMQ_URL` (routing exclu) ; auth-api via `DATABASE_URL` (URL) ; PORT forcé par env[].value (défaut code 8080) ; auth-api/graphql-api `command:["node","/usr/local/bin/<pkg>"]` car node.dockerfile dist sans ENTRYPOINT ; platform-app CMD du react.dockerfile. **`.github/workflows/ci.yml`** (ubuntu-latest) : job go (`go build ./src/...` + `go vet ./src/...` + `go test ./src/...`, Go 1.26.4 aligné go.mod), job ts (`make build pkg=auth-api` + `make build pkg=graphql-api`, npm ci), job migrations (`atlas migrate hash --dir file://migrations` + drift git diff + `atlas migrate validate`), job frontend (`make build pkg=platform-app`). **QA PASS A-G** : 16/16 fichiers, 15/15 YAML parsés (python3+pyyaml) 0 tabulation, triple cohérence ports/DNS 8/8, env conformes (double DSN POSTGRES_DSN+DATABASE_URL), job Atlas câblé, images `fleece/<svc>:latest`, CI complète. **PM** : correction d'un commentaire trompeur `routing.yaml` l.47 (RABBITMQ_URL injecté via envFrom fleece-config mais non consommé par routing). **Offline** : kubectl/docker/atlas non exécutés (cluster/réseau absents) → validation runtime en CI online + 1er déploiement. **Dettes** (voir Blocages T-009) : `make build pkg=rest-api` absent du CI (hors-scope) ; storage emptyDir dev ; ConfigMap migrations créé impérativement (à automatiser) ; NEXT_PUBLIC_GRAPHQL_URL = DNS interne (à pointer vers le LB externe pour le navigateur). **À reporter dans MEMORY.md** : (a) `deploy/k8s/` MVP = manifests YAML bruts (pas de Helm), namespace `fleece`, organisation infra/config/jobs/services ; (b) **convention double connexion DB** : `POSTGRES_DSN` (libpq key=value) pour les services Go ET `DATABASE_URL` (URL postgres://) pour auth-api + Atlas — les deux dans le Secret, le search_path par service via `POSTGRES_SEARCH_PATH` (isolation schéma D11) ; (c) init job Atlas = image bastion + ConfigMap `atlas-migrations` monté /migrations + `atlas migrate apply` (le bastion ne copie pas le repo) ; (d) noms de Service K8s = identifiants DNS load-bearing (doivent matcher exactement les *_API_URL/*_URL) ; (e) auth-api/graphql-api nécessitent `command:["node",...]` car node.dockerfile dist n'a pas d'ENTRYPOINT ; (f) CI GitHub Actions = go (build/vet/test) + ts (make build auth-api/graphql-api) + migrations (atlas hash) + frontend (make build platform-app).
- **2026-06-22** — T-008 **Done** (PASS). Dashboard `src/platform-app` (Next.js App Router, TS) livré. Séquence : devops-engineer (prérequis build) → frontend-engineer → qa + architect-reviewer (parallèle) → PM. **Prérequis devops** : `mk/react.mk` (cibles calquées sur node.mk ; `build` = `tsc --noEmit` + `bash -c 'cd ./src/${pkg} && npm exec -- next build'`, `target=dist`, `dockerfile=docker/react.dockerfile`), `docker/react.dockerfile` (multi-stage Next `output:'standalone'` : build `make build pkg=$PKG`, dist copie `.next/standalone`+`.next/static`, `CMD node server.js`), `./src/platform-app` ajouté aux workspaces npm ; `make --dry-run` build/image/test OK, Dockerfile parsé OK. **Frontend** : 34 fichiers écrits à la main (offline, sans create-next-app) — config (`next.config.js` `output:'standalone'`, tsconfig `@/*`, `@fleece/platform-app`), couche données `lib/graphql/` (client `fetch`→BFF via `NEXT_PUBLIC_GRAPHQL_URL`, 0 dép Apollo/urql, types alignés SDL), UI inline shadcn-like (`components/ui/*`, `components/states/*`), Sidebar 230px + Header, `globals.css` tokens DESIGN.md exacts (dark défaut). 4 écrans : DASH-01 signup + onboarding stepper 4 étapes (rawKey 1×), DASH-02 api-keys (liste/create modal rawKey 1×/revoke/rotate=revoke+create), DASH-03 wallet (solde centimes/100+currency/recharger placeholder/historique empty-state), DASH-04 webhooks (liste/add url+events/delete/secret masqué/livraisons empty-state). **QA PASS** (A 34/34 fichiers + `output:'standalone'`, B 0 import backend + types↔SDL + 0 appel GraphQL fantôme, C DASH-01..04 + dark theme 1:1 DESIGN.md, D tsc SKIP offline). **architect-reviewer CONFORME** (données 100% via BFF GraphQL, 0 import Drizzle/Better Auth/pg/services Go, `type=react` intact, appels `/api/auth/*` same-origin = non bloquant). **Contrainte offline respectée** : aucun `npm install`/`next build`/`tsc`. **Dettes** (voir Blocages T-008) : opérations BFF manquantes (rotateApiKey, Query.transactions, webhook secret/deliveries, top-up, createWorkspace) traitées en UI placeholder/empty-state sans appel fantôme ; outillage (build/image/shadcn/fonts à valider online) ; chemin `server.js` standalone monorepo à vérifier au 1er `make image` ; workspaceId session à brancher ; runtime BFF (Apollo/Yoga) prérequis pour données réelles. **À reporter dans MEMORY.md** : (a) support build `react` ajouté (`mk/react.mk` + `docker/react.dockerfile` Next standalone) → pattern Next App Router dans le système Makefile maison ; (b) frontend Clean Architecture frontière = données exclusivement via couche `lib/graphql/` (fetch→BFF), 0 import backend, auth Better Auth via routes same-origin `/api/auth/*` hors GraphQL ; (c) approche offline scaffold Next manuel (sans create-next-app) + UI shadcn stubs inline + types GraphQL alignés SDL à la main, même esprit que T-006/T-007.
- **2026-06-19** — **Remboursement des dettes résiduelles D1/D2/D3/D4** — **PASS / CONFORME**. Inventaire complet du tracker → 4 dettes catégories A (code pur) + B (schéma+code) traitées ; catégorie C (CI/outillage : `atlas migrate hash`/`lint`, `npm install`, dependency-cruiser) skippée (hors ligne, non bloquante). **D1 ValidateSession workspaceId** (CRITIQUE, ts-engineer) : use case C2 `validate-session.ts` (authProvider.validateSession → userRepository.findById → ApiContext avec workspaceId réel), `POST /validate-session` délègue au use case, `graphql-api` fetch ACTIVÉ ; QA FAIL 1er tour (mappeur `UnauthorizedError`→403) → correctif 401 → re-PASS. **D2 Provider DLR status** (IMPORTANT, db→go) : migration additive `0012_provider_dlr_status.sql` (+status/updated_at) + port `UpdateStatus` + `handle_dlr.go` persiste le statut. **D3 Routing Fastest/Custom** (MOYEN, db→go) : migration additive `0013_routing_strategies.sql` (+avg_latency_ms/country/channel/config JSONB), `selector.go` implémente Fastest (tri latence) et Custom (Config.preferred_order), décodage JSONB confiné C3, `SelectProvider` reçoit RoutingRule. **D4 permissions** (MINEUR, ts) : `schema.ts` aligné `text().array()`. Cycle global : db-engineer ×2 + ts-engineer (parallèle) → go-engineer ×2 → qa + architect-reviewer. **Vérifs** : `go build ./src/...` OK (0 régression 15 pkgs), `go test ./src/provider/... ./src/routing/...` 54 PASS, go.mod/go.sum inchangés (stdlib-only), 0 import tiers actif ; tsc système absent → type-check TS SKIP documenté + revue statique ; better-auth/drizzle 100% stubés/commentés (offline respecté). **architect-reviewer CONFORME** sur les 3 blocs (provider/routing/TS) : ports C2/impl C3, domaine pur (encoding/json confiné C3, RoutingConfig=map type pur), frameworks confinés, isolation schémas provider/routing/identity, composition roots C4→C3→C2. **Reste CI** : `atlas migrate hash --dir file://migrations` (0012+0013, offline), `npm install`+`make build pkg=auth-api|graphql-api` (esbuild), dependency-cruiser. **À reporter dans MEMORY.md** : (a) pattern « migration additive 00NN + adapter + activation méthode/use case » réutilisé une 3e/4e fois (après webhook 0010, identity 0011) → désormais le pattern de référence pour solder une dette schéma ; (b) décodage JSONB en Clean Architecture = type pur (`map[string]any`) dans le domaine + `encoding/json` strictement en adapter persistence ; (c) ValidateSession workspaceId résolu via `user.workspaceId` (le User porte son workspace, pas de WorkspaceRepository.findByOwnerId nécessaire) — débloque le contrat sessions dashboard (T-008) ; (d) leçon QA : un mappeur d'erreurs HTTP global peut masquer une incohérence (UnauthorizedError→403) même quand le chemin nominal est correct → vérifier la table errorToStatus, pas seulement le handler.
- **2026-06-14** — **Remboursement dette T-006 (schéma identity) + dette T-007 (`/validate-session`)** — **PASS / CONFORME**. Migration additive `migrations/0011_identity_schema.sql` (db-engineer ; 0010 déjà pris par webhook → 0011) : 10 colonnes `ADD COLUMN IF NOT EXISTS` sur `identity.workspaces` (slug/owner_id/plan), `identity.users` (name/role), `identity.api_keys` (name/prefix/permissions TEXT[]/last_used_at/expires_at) ; UNIQUE sur slug via `CREATE UNIQUE INDEX IF NOT EXISTS uq_workspaces_slug` ; 0 modif de 0001, un seul schéma `identity`. Alignement TS (ts-engineer, 9 fichiers) : `infrastructure/db/schema.ts` (colonnes Drizzle alignées 0011), `workspace/user/api-key-repository.ts` (mapping bidirectionnel, défauts à la lecture + TODO(schema) retirés), **`updateLastUsed` n'est plus un no-op** (UPDATE Drizzle commenté + throw explicite offline) et est appelé par `validate-api-key.ts` (l.42, chemin succès), **route `POST /validate-session`** dans `auth.handler.ts` (Bearer header prioritaire + fallback body `{token}` → `authProvider.validateSession` C2 → 200 ApiContext `{workspaceId,userId,authMethod:"session"}` ou 401, `authProvider` injecté + câblé dans `index.ts`), commentaires obsolètes domain/ nettoyés. Cycle : db-engineer → ts-engineer (`tsc --noEmit` exit 0 auth-api + graphql-api) → qa (**PASS** : 10/10 colonnes alignées, 0 import npm actif non installé, updateLastUsed réel+appelé, /validate-session conforme, mocks OK, jest SKIP offline) + architect-reviewer (**CONFORME** : persistence C3, Drizzle/Better Auth confinés, updateLastUsed orchestré C2 via port, /validate-session délègue au port AuthProvider à types purs, domaine pur, isolation schéma identity, composition root C4→C3→C2). **Contrainte offline respectée** : aucun `npm install`, drizzle-orm/better-auth 100% commentés/stubés. **Dettes résiduelles documentées** : (a) `/validate-session` renvoie `workspaceId: ""` (le port `SessionData` ne porte pas le workspaceId → use case `ValidateSession` à créer qui résout userId→workspaceId via UserRepository) ; (b) `permissions: text()` au lieu de `text().array()` dans le stub Drizzle (à corriger au branchement réel) ; (c) côté graphql-api `HttpSessionValidator.validate` reste un stub `null` (fetch à décommenter). **Dette outillage CI signalée par l'architect-reviewer** : aucun fichier `.dependency-cruiser` à la racine — le garde-fou TS automatisé des frontières (D14/§7) n'est pas encore présent (→ tâche devops à planifier). Reste CI : `atlas migrate hash --dir file://migrations` + `atlas migrate lint` (offline localement, pas d'`atlas.sum`), `npm install` + `make build pkg=auth-api` (esbuild). **À reporter dans MEMORY.md** : dette schéma identity (0001) soldée via le pattern « migration additive 00NN + alignement schema.ts/repos + activation de la méthode jusque-là no-op » (réutilisé après webhook T-005) ; `/validate-session` ajouté côté auth-api débloque la dépendance T-008 (sessions dashboard) côté contrat, mais le runtime reste à activer (stubs offline + résolution workspaceId).
- **2026-06-14** — **Remboursement dette T-005 (schéma webhook)** — **PASS / CONFORME**. Migration additive `migrations/0010_webhook_schema.sql` (db-engineer) : `webhook_endpoints.active BOOLEAN NOT NULL DEFAULT true`, `webhook_deliveries.payload BYTEA`, `webhook_deliveries.next_retry_at TIMESTAMPTZ`. Adapter persistence + composition root mis à jour (go-engineer) : `record.go` mappe Active/Payload/NextRetryAt (plus de `true` forcé, TODO schéma retirés), repos persistent les nouvelles colonnes, nouveau port `DeliveryRepository.FindPendingRetries(ctx, at)` (C2) + implémentation Postgres (C3, filtre `next_retry_at <= at AND status='failed'`), **scheduler de retry câblé** dans `main.go` (ticker 60s → FindPendingRetries → `retryUC.Execute(ctx, d.ID)`, arrêt `ctx.Done()`, no-panic si `db==nil`). QA : 48 tests webhook PASS (+ `persistence/record_test.go`), `go build ./src/...` OK (0 régression). Architect-reviewer : CONFORME (inversion port, 0 violation C3→C4, domaine pur, scheduler = câblage pur, isolation schéma). PM : nettoyage des commentaires obsolètes (domain/delivery.go, usecases/retry_delivery.go). **Stdlib-only, go.mod/go.sum inchangés.** Reste à faire en CI : `atlas migrate hash --dir file://migrations` (Atlas indisponible offline ; pas encore d'`atlas.sum`). **À reporter dans MEMORY.md** : dette schéma webhook soldée → scheduler de retry désormais durable (payload + next_retry_at persistés) ; pattern « migration additive 0010 + alignement adapter + câblage scheduler au composition root » réutilisable pour la dette schéma identity (0001) encore ouverte.
- **2026-06-14** — T-007 **Done** (PASS). BFF GraphQL (`src/graphql-api`, TS) implémenté par ts-engineer : C2 application (ports/output/clients.ts = 4 ports IdentityClient/WalletClient/MessagingClient/WebhookClient avec `ctx: ApiContext` en 1er param + 7 DTO purs + `Page<T>` de @fleece/api-common pour les listes paginées ; use cases get-workspace-overview agrégation `Promise.all`(getWorkspace+getBalance), list-api-keys, manage-webhook list/create/delete) ; C3 adapters (graphql/schema.graphql MVP complet — Workspace/ApiKey/WalletBalance/Transaction/Message/WebhookEndpoint, PageInfo cursor + MessagePage/TransactionPage, 5 Query + 4 Mutation ; context.ts buildContext framework-agnostic extrait Bearer/cookie→SessionValidator→ApiContext, ApiError UNAUTHORIZED ; 3 résolveurs workspace/wallet/webhook délégant aux use cases/clients + index buildResolvers fusion {Query,Mutation} ; 4 clients REST identity/wallet/messaging/webhook stubs `fetch` global commenté + TODO(production) + assertions `_conformance`) ; C4 infrastructure (config.ts port 4000 + AUTH/WALLET/MESSAGING/WEBHOOK_API_URL depuis env, session-validator.ts HttpSessionValidator implémente le port SessionValidator de api-common via auth-api /validate-session stub + _conformance, server.ts stub GraphQLServerLike répondant 501 mais câblage réel schéma+resolvers+contextFn en commentaire TODO, node:http + SIGTERM) + graphql.d.ts (declare module "*.graphql" pour tsc, schéma chargé en texte par esbuild) ; composition root index.ts DI manuelle (config→clients→sessionValidator→use cases→resolvers→server). tsconfig.json : ajout baseUrl + paths @fleece/api-common (calque auth-api). **Contrainte offline respectée** : Apollo/Yoga/graphql/graphql-tag 100% commentés (TODO production), seuls `fetch` global et `node:*` actifs, aucun npm install. Cycle : ts-engineer (`tsc --noEmit` exit 0 via tsc système 5.9.3, 1 bug TS2693 corrigé en cours, auth-api sans régression) → architect-reviewer (**CONFORME**, 0 violation : application/ pure, SessionValidator confiné infrastructure, clients en adapters/clients avec inversion, résolveurs sans logique métier, SDL↔resolvers alignés, offline OK) + qa (**PASS** global : A 20/20 fichiers, B interfaces/imports cohérents, C schéma valide 12/12 accolades, D garde-fous offline, E tsc exit 0). Pas de couche domain/ (BFF orchestration pure, D18). **Dettes documentées** (→ Blocages) : Apollo/Yoga à installer+décommenter pour runtime ; stubs clients retournent null/vide ; `/validate-session` à ajouter côté auth-api (dépendance T-008, sinon 401) ; `TransactionPage` sans Query.transactions (DASH-03) ; package `@anthill/graphql-api`→`@fleece/` ; token service-to-service TODO. **À reporter dans MEMORY.md** : pattern BFF GraphQL Clean Architecture (ports clients en C2 + DTO purs, context/résolveurs/clients en C3 adapters/graphql + adapters/clients, SessionValidator en C4, framework GraphQL stubé hors application) ; même approche offline que T-006 (stubs npm commentés, tsc via tsc système, paths béquille).
- **2026-06-14** — T-006 **Done** (PASS). Identity Service (`src/auth-api`, TS) implémenté par ts-engineer : C1 domaine pur (Workspace enrichi, ApiKey avec keyHash/prefix/permissions/active/lastUsedAt/expiresAt + isExpired/isActive/revoke, User interface, 5 errors WorkspaceNotFound/ApiKeyNotFound/ApiKeyRevoked/InvalidApiKey/Unauthorized), C2 ports output (Workspace/ApiKey/User Repository + port AuthProvider createSession/validateSession/deleteSession à types purs) + use cases create-workspace (Workspace + owner User), create-api-key (format `fk_<prefix>_<random>`, hash SHA-256 via node:crypto, ne stocke que le hash, retourne la clé claire une seule fois), validate-api-key (hash + findByHash + isActive + updateLastUsed → ApiContext de @fleece/api-common, authMethod api_key), revoke-api-key, rotate-api-key (revoke + create), C3 adapters/auth (BetterAuthProvider implémente AuthProvider, **Better Auth confiné**, BetterAuthLike stub + TODO offline) + persistence Drizzle (workspace/api-key/user repository, mapping colonnes exactes 0001, assertions `_conformance` du port) + http/auth.handler.ts (node:http stdlib, routes POST /workspaces, POST /workspaces/:id/api-keys, DELETE .../:keyId, POST .../rotate, POST /validate-key, mapping erreurs domaine→HTTP 400/401/403/404), C4 infrastructure/db/schema.ts (pgSchema identity aligné 0001, Atlas reste propriétaire des migrations) + better-auth.config.ts (stub) + composition root index.ts (DI manuelle, serveur node:http, SIGTERM). Cycle : ts-engineer (tsc --noEmit OK via tsc système) → architect-reviewer (CONFORME : domaine/application purs, Better Auth + Drizzle confinés C3/C4, inversion par ports, aucun import npm actif non installé) → qa (PASS structurel : 18/18 fichiers, 6/6 fonctionnels, 4/4 garde-fous ; tsc/jest SKIP documenté offline). **Contrainte offline respectée** : better-auth (non listé) et drizzle-orm/pg (listés mais node_modules absent) entièrement stubés/commentés, aucun npm install. Style imports `.js` (NodeNext) appliqué partout (fichiers existants retouchés). **Dette schéma 0001 documentée** (→ db-engineer) : 10 colonnes d'entités non persistables → migration `0010_identity_*` à planifier avant production (lastUsedAt no-op, permissions/expiration non durables). **Observations non bloquantes** : `paths` tsconfig (béquille offline, divergence esbuild/tsc à surveiller) ; package nommé `@anthill/auth-api` à harmoniser en `@fleece/`. Build esbuild `make build pkg=auth-api` non exécuté (offline) → à re-valider en CI online. **À reporter dans MEMORY.md** : pattern Identity TS Clean Architecture (port AuthProvider à types purs, Better Auth confiné adapters/auth, Drizzle confiné persistence/infra) ; dette schéma identity = arbitrage migration vs domaine riche (même pattern que T-004/T-005).
- **2026-06-14** — T-005 **Done** (PASS). Service Webhook implémenté par go-engineer : C1 domaine pur (WebhookEndpoint+Subscribes, WebhookDelivery, DeliveryStatus Pending/Delivered/Failed/Exhausted, signature HMAC-SHA256 Sign/Verify stdlib avec hmac.Equal temps constant, errors + ErrEmptyPayload/ErrMaxRetriesExceeded/ErrInvalidSignature/ErrEndpointNotFound), C2 ports input/output (EndpointRepository/DeliveryRepository/HTTPDispatcher) + usecases DeliverEvent (signe + dispatch endpoints actifs abonnés, 2xx→Delivered/non-2xx→Failed), RetryDelivery (backoff exponentiel 1m/5m/30m/2h/8h via backoffFor testable, max 5→Exhausted), RegisterEndpoint (validation url/events, secret crypto/rand), C3 persistence (schéma `webhook`, text[] encodé manuellement faute de driver) + dispatcher net/http (header X-Fleece-Signature, timeout 10s) + consumer RabbitMQ (message.delivered/failed, wallet.debited/refunded) + interface Broker en C3 (`adapters/webhook/broker.go`) + http handler/dto (POST/GET/DELETE /endpoints), C4 config(8085)/postgres/rabbitmq(NoopBroker avec assertion)/httpserver, composition root DI manuelle (retryUC non encore câblé à un scheduler). Cycle : go-engineer → qa (PASS, 41 tests dont +28 ajoutés par QA dont la vérif que DeliverEvent émet un HMAC réellement vérifiable) → architect-reviewer (CONFORME) → go-engineer (correctifs non bloquants : assertions repos + guard ErrEmptyPayload) → re-test 29/29 PASS. Stdlib-only, go.mod inchangé / pas de go.sum. **Dette schéma documentée** (→ db-engineer) : 0006 minimal (sans payload/next_retry_at/active) bloque un scheduler de retry réel ; migration à aligner avant câblage. **Décision technique à reporter dans MEMORY.md** : pattern Broker C3 confirmé pour webhook (interface dans `adapters/webhook` cette fois, pas `adapters/messaging`) ; dette schéma webhook = arbitrage migration vs domaine.
- **2026-06-13** — Création du tracker + de l'équipe d'agents. Baseline scaffold consignée. Backlog MVP T-001..T-010 défini.
- **2026-06-13** — T-001 **Done** (PASS). Adapters concrets messaging implémentés par go-engineer (C3 persistence/publisher/clients/http/consumer + C4 config/postgres/rabbitmq/httpserver + composition root). Cycle: go-engineer → qa (PASS build/vet/tests) → architect-reviewer (FAIL: 2 violations C3→C4) → go-engineer (fix interface Broker → C3) → architect-reviewer (CONFORME). Stdlib-only conservé (pas de go.sum), conforme à la contrainte offline du dépôt.
- **2026-06-13** — T-004 **Done** (PASS). Service Provider implémenté par go-engineer : C1 domaine (OutboundMessage, Money value object, DeliveryStatus Pending/Sent/Delivered/Failed/Rejected avec transitions, Provider, errors ProviderUnavailable/SendFailed/ProviderNotFound), C2 port central `Provider` (Send/EstimateCost/GetDeliveryStatus) + ProviderRepository/EventPublisher + usecases Dispatch (registry par providerId, persiste provider_messages, publie provider.sent/failed) et HandleDLR (publie provider.delivered/failed), C3 adapters providers WhatsAppMeta + SMSTwilio (stubs net/http stdlib, TODO(production), pas de réseau réel) + persistence (schéma `provider`) + publisher + interface Broker C3 + http (POST /dispatch, POST /dlr), C4 config(8084)/postgres/rabbitmq(NoopBroker)/httpserver, composition root DI manuelle. Cycle : go-engineer → qa (PASS, 35/35 tests, +28 tests ajoutés) → architect-reviewer (CONFORME du 1er coup). Stdlib-only, go.mod inchangé / pas de go.sum. **Écart documenté** : schéma 0005 minimal (providers id/channel/enabled ; provider_messages internal_id/provider_id/external_id seulement) → entités plus riches (Name/Country) non persistées, Active↔enabled, pas de colonne status → le statut DLR est publié en événement sans persistance (TODO). Recommandations archi non bloquantes : assertions `var _ output.Provider` dans les adapters, vérifier couverture depguard en CI.
- **2026-06-13** — T-003 **Done** (PASS). Service Routing implémenté par go-engineer : domaine pur (Money, Channel, RoutingStrategy lowest_cost/highest_delivery/fastest/custom, ProviderPricing/Score, RoutingRule, RoutingDecision+ProviderRef, `SelectProvider` cœur métier), use cases get_routing_decision (rule absente → défaut highest_delivery) / update_provider_score (feedback DLR, Upsert ON CONFLICT (provider,channel)), adapters persistence (schéma `routing`, tables provider_pricing/provider_scores/routing_rules de 0004) + http (POST /route, POST /scores), config (port 8083, DefaultCurrency XAF) + postgres + httpserver, composition root sans broker. Cycle : go-engineer → qa (PASS, 35/35 tests, 17 tests handler ajoutés) → architect-reviewer (CONFORME du 1er coup). Stdlib-only, go.mod inchangé / pas de go.sum. **Écart documenté** : le schéma 0004 est plus simple que la spec domaine idéale (provider_scores sans country/delivery_rate/avg_latency ; routing_rules sans channel/config) → adapters alignés sur le schéma réel ; `Fastest` et `Custom` retombent sur le tri par score faute de colonnes latence/config (TODO en commentaire). Devise dérivée de la config car absente de provider_pricing. Service Wallet implémenté par go-engineer : domaine Money (value object centimes)/Wallet/WalletTransaction, use cases Debit/Credit/Refund/GetBalance, adapters persistence (schéma `wallet`, tables wallets + wallet_transactions ledger append-only)/publisher/http (GET /balance, POST /debit|credit|refund), interface Broker en C3, NoopBroker + infra en C4, composition root. Cycle: go-engineer → qa (PASS, 19/19 tests) → architect-reviewer (CONFORME du 1er coup). Stdlib-only, go.mod/go.sum inchangés. Dette de tests notée (use cases credit/refund/get_balance + adapters non couverts).
