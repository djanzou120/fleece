{
  "name": "@fleece/graphql-api",
  "version": "0.0.0",
  "private": true,
  "description": "GraphQL Gateway (BFF) — sert le dashboard, agrège les services via REST interne.",
  "dependencies": {
    "@fleece/api-common": "*"
  }
}
