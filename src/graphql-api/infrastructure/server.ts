// Couche 4 — Serveur GraphQL (Apollo / Yoga). Détail d'infrastructure.

export function startServer(): void {
  // TODO: instancier le serveur GraphQL avec le schéma et les résolveurs,
  // appliquer la vérification de session (déléguée à auth-api / Better Auth).
  console.log("graphql-api (BFF) server bootstrap");
}
