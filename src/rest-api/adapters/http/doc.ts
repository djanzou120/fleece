// Couche 3 — Adapteurs HTTP (driving).
// Chaque fichier de ce dossier contient les routes d'une ressource REST :
//   messages.routes.ts  → POST /v1/messages
//   wallet.routes.ts    → GET/POST /v1/wallet
//   webhooks.routes.ts  → GET/POST/DELETE /v1/webhooks
//
// Responsabilité : traduire la requête HTTP → appel de port de sortie,
// formater la réponse. Aucune règle métier ici.
export {};
